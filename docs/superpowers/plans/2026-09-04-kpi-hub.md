# KPI Hub Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship KPI Hub v1.1 — workspace từ điển + target + cảnh báo + chất lượng dữ liệu + báo cáo Marketing & Sales, UI **trùng mockup đã đính kèm**, API `/api/crm/kpi-hub`, không thay `/crm/kpi` hay KPI Type.

**Architecture:** Nest module `kpi-hub` cạnh `kpi-types`. Fact materialize `crm_kpi_facts` (Dashboard không query Ads live). Formula AST allowlist + connector port tái sử dụng Lead/Meta. Frontend là **shell riêng** `KpiHubShell` (sidebar 7 mục như mockup), CSS `.kpi-hub-*`, không Tailwind. UI dựng bằng fixture trước, rồi gắn API.

**Tech Stack:** NestJS (`ptt-crm-api`), PostgreSQL, Next.js 14 (`ops-web`), Jest + Vitest + Playwright, `globals.css`.

**Spec:** `docs/superpowers/specs/2026-09-04-kpi-hub-srs.md` (v1.1).  
**Mockup gốc (bắt buộc đối chiếu từng màn):**

| Màn | File ảnh |
|---|---|
| Dashboard | `generated-image-19-e5b6ad5c-516c-4e00-b607-c1f973b798f4.jpg` |
| Dictionary list + drawer | `generated-image-14-48b8cd47-6555-462d-aecb-271cb7877bf6.jpg` |
| Công thức & Logic | `generated-image-16-50e49fb2-8890-4868-b0c9-d583d6525aa5.jpg` |
| Data Source Mapping | `generated-image-17-20575e33-d9da-4f90-9703-08c7a1aaddb6.jpg` |
| Target & Cảnh báo | `generated-image-15-16aa51ec-3df3-4551-a5da-f108de49c09c.jpg` |
| Data Quality | `generated-image-18-128343f4-bd81-48a6-8d25-1bca6f6d5da0.jpg` |
| Báo cáo | `generated-image-20-e192dff4-4b28-4ea4-bb7e-eea456b777e6.jpg` |
| Cài đặt | `generated-image-21-9f422fc7-4e4a-4443-8707-a7087a6d1524.jpg` |

Đường dẫn ảnh session: `/Users/quoctuan/.cursor/projects/Users-quoctuan-Documents-CursorAI-RNOSAI/assets/`. Copy vào `docs/superpowers/mocks/kpi-hub/` khi implement để agent luôn mở được.

## Global Constraints

- **UI = mockup.** Cùng sidebar, cùng thứ tự khối, cùng nhãn tiếng Việt, cùng màu emerald / cam / đỏ / xám. Không bịa layout “gần giống”. Không Tailwind. Prefix CSS `kpi-hub-*`.
- KPI Hub **tự có sidebar** 7 mục. Không nhét Hub vào OpsNav 2 cấp (sai mockup). OpsNav chỉ thêm 1 link **KPI Hub** → `/crm/kpi-hub`.
- Không thay `/crm/kpi`, `/crm/kpi/groups`, `/crm/kpi/types`.
- Tenant isolation: `tenant_id = 'PTT'` + `workspace_id`. Query repository bắt buộc cả hai + `deleted_at IS NULL`.
- Dashboard live chỉ đọc Dictionary `ACTIVE`. Null → `—`, không ghi 0 giả. Ratio non-additive: `sum(num)/sum(den)`.
- `DATA_ISSUE` (source Failed / DQ Critical) ưu tiên hơn badge Đạt khi workspace bật precedence.
- `code` immutable sau Publish; format `^[A-Z]{2,5}_[A-Z0-9_]+$`.
- Sửa formula/source/unit/direction của Active → version mới, không hồi tố fact kỳ khóa.
- Optimistic lock: `If-Match: {row_version}` → `409` + `KPI_HUB_VERSION_CONFLICT`.
- Secret connector không ra API/UI/log. Sample DQ mask PII.
- RBAC backend + `rbac-routes.ts`. Cap theo SRS §21.
- P1 không làm: AI, forecast, SSO, Slack, Data-Driven attribution, Power BI live (chỉ export dim/fact P2+).
- Pattern copy: `services/ptt-crm-api/src/kpi-types/`, `services/ops-web/src/components/kpi-types/`.
- TDD: hàm thuần (status, ratio, freshness, cycle) viết test trước.

---

## UI fidelity bible (không được lệch)

Mọi trang Hub dùng một khung:

```text
┌ KpiHubShell ─────────────────────────────────────────────┐
│ Sidebar 240px │ Header (breadcrumb + search/bell + PTT)  │
│ KPI Hub       │ Page header (title + subtitle + actions) │
│ 7 menu items  │ Main (cards / table / widgets)           │
│ Thu gọn       │ [optional right rail]                    │
│               │ Footer freshness (Dashboard, Báo cáo)    │
└───────────────┴──────────────────────────────────────────┘
```

- Sidebar nền trắng, item active: nền xanh lá nhạt + chữ `#059669` + icon.
- Primary button: nền `#10B981`, chữ trắng, bo 8–10px.
- Card: nền trắng, border `#E5E7EB`, bo 12px, bóng nhẹ.
- Badge Đạt/Active/Fresh = xanh; Cảnh báo/Need Review/Delayed = cam; Nguy cấp/Failed = đỏ; Draft = xám.
- Font hệ thống ops-web hiện tại (không đổi font mới).
- Desktop-first ≥ 1280px. Cột phải drawer ~360–400px.

### Sidebar — copy đúng

1. Dashboard  
2. KPI Dictionary  
3. Target & Cảnh báo  
4. Nguồn dữ liệu  
5. Data Quality  
6. Báo cáo  
7. Cài đặt  

Đáy: nút `Thu gọn`. Logo chữ **KPI Hub** + icon bar chart.

### Màn Dashboard (ảnh 19)

- Breadcrumb: `Tổng quan / Dashboard`
- Title `Dashboard` · subtitle `Tổng quan hiệu quả Marketing & Sales`
- Actions: date `01–30 Tháng 09, 2026` · `So sánh kỳ trước` · icon xuất · nút xanh `Tạo báo cáo`
- Filter: `Toàn bộ phòng ban` · `Tất cả kênh` · `Tất cả sản phẩm` · `Tất cả team` · link `Đặt lại bộ lọc`
- 5 thẻ ngang: Doanh thu ký mới / Tổng Valid Leads / CPL Valid Lead / MQL Rate / Win Rate — số lớn, sparkline, badge target
- Hàng 2: Funnel ngang 6 tầng + callout **Điểm nghẽn** · donut `68% Tổng thể` + 4 bar nhóm
- Hàng 3: bar kênh Valid Leads+Revenue · Cảnh báo cần xử lý · Top Sales
- Footer: `Dữ liệu cập nhật: Hôm nay, 08:45` · CRM Fresh · Meta Ads Fresh · SharePoint Delayed

Fixture AC-02: doanh thu `1.24 tỷ đ` +18.6%; Valid Leads `1,486` +12.4%; CPL `142.000 đ` Đạt ≤ 150.000; MQL `24.8%` thiếu 5.2%; Win `12.5%` Nguy cấp ≥ 20%. Funnel 2340→1486→369→152→86→19. Top #1 Nguyễn Minh Anh 420 triệu / 18.7%.

### Màn Dictionary (ảnh 14)

- Breadcrumb `Quản trị dữ liệu / KPI Dictionary`
- 4 thẻ: Tổng KPI 22 · Đang hoạt động 20 · Cần rà soát 1 · Nguồn dữ liệu 7
- Search `Tìm KPI, mã KPI, nguồn dữ liệu...` · lọc Nhóm KPI / Data Owner / Trạng thái · `+ Tạo KPI`
- Cột: KPI ID, Tên Metric, Nhóm KPI (badge màu), Nguồn, Tần suất, Data Owner, Trạng thái, ⋮
- Click hàng → drawer phải: CPL Valid Lead, `MKT_006`, Active, phân số chi tiêu / Valid Leads, target `≤ 150.000 VND`, nút `Chỉnh sửa KPI`

### Màn Công thức (ảnh 16)

- Breadcrumb `KPI Dictionary / CPL Valid Lead / Chỉnh sửa`
- Nút `Lưu nháp` + `Lưu & Xuất bản`
- 5 tab ngang: Tổng quan · **Công thức & Logic** · Nguồn dữ liệu · Target · Governance
- Loại phép tính `Ratio` · card Tử số `MKT_004` · card Mẫu số `MKT_002`
- Ô xanh công thức nghiệp vụ · khối DAX `DIVIDE(...)`
- 3 toggle: BLANK mẫu số 0 ON · không cộng dồn tỷ lệ ON · nhập thủ công OFF
- Cột phải: Quy tắc thời gian · Kiểm tra logic · KPI phụ thuộc

### Màn Mapping (ảnh 17)

- Cùng header/tab, tab **Nguồn dữ liệu** active
- Bảng 3 nguồn: Meta Ads Numerator Connected · CRM Denominator Connected · SharePoint Lookup Delayed
- 2 card mapping chi tiết + preview `SUM` / `DISTINCTCOUNT`
- Cột phải: chiến lược Campaign + Date, map `campaign_id → utm_campaign`, toggle UTM + mapping table, lineage, quality 96%, `12 campaign chưa được mapping`

### Màn Target (ảnh 15)

- Title `Target & Cảnh báo` · subtitle đúng SRS
- `+ Thiết lập Target` (xanh) · `Quy tắc cảnh báo`
- 4 thẻ: 18/22 · 12 (66%) · 4 (22%) · 2 (11%)
- Bảng Actual/Target/Warning/Critical/Trend/Status
- Drawer CPL: Lower is better, 09/2026, gauge, alert 4 giờ, Email + Teams

### Màn Data Quality (ảnh 18)

- `Xuất báo cáo` · `Chạy kiểm tra`
- Gauge 92/100 · 5/7 · 3 warning · 1 critical
- Chart xu hướng + freshness 4 nguồn
- Bảng rule + drawer Lead_ID 12 lỗi · `Gán người xử lý` · `Tạo ticket`

### Màn Báo cáo (ảnh 20)

- `Thư viện mẫu` · `Lịch gửi báo cáo` · `+ Tạo báo cáo`
- 4 thẻ: 12 / 6 / 2 / 28
- Tab Tất cả / Của tôi / Đã chia sẻ / Lịch gửi
- Cột phải: 4 thẻ tạo nhanh · lịch gửi tiếp theo · chia sẻ gần đây
- Footer freshness như Dashboard

### Màn Cài đặt (ảnh 21)

- Nav con 9 mục, mặc định **Không gian làm việc**
- Form org + chu kỳ Tháng / chốt ngày 03 / đối soát ngày 05
- Danger zone 2 nút đỏ
- Cột phải: tình trạng hệ thống + 4 toggle nhanh

**Cổng nghiệm UI:** mỗi sóng frontend phải chụp/so với ảnh trên. Lệch khối, thiếu widget, sai nhãn = fail task.

---

## Hiện trạng (gap)

| Hạng mục | Hiện có | Cần làm |
|---|---|---|
| Sổ điểm NV | `/crm/kpi` | Không đụng |
| Nhóm KPI / KPI Type | CRUD + formula DSL | Hub Dictionary **tham chiếu** Type, không thay |
| Connector | `crm_leads`, `daily_performance` | Binding Hub + SharePoint lookup table |
| Hub route/UI | Không | Shell + 8 màn |
| Fact / Target hierarchy | Không | `crm_kpi_facts`, `crm_kpi_period_targets` |
| DQ / Report / Workspace | Không | Module mới |

---

## Lộ trình 8 sóng

| Sóng | Phạm vi | AC |
|---|---|---|
| **W0 — Shell + token** | `KpiHubShell`, 7 route trống đúng chrome, nav OpsNav, RBAC stub, CSS token | AC-01 |
| **W1 — DDL + domain thuần** | SQL, types, status/ratio/freshness/cycle tests, workspace row mặc định | AC-09 logic, AC-16 |
| **W2 — Dictionary** | List+drawer+editor 5 tab **pixel**, CRUD+publish API, seed 21 KPI | AC-05, AC-06, AC-21, AC-22 |
| **W3 — Formula + Mapping** | Tab formula/mapping đúng ảnh 16–17, parser RATIO, validate, sources catalog | AC-07, AC-08, AC-24, AC-30 |
| **W4 — Target + Alert** | Ảnh 15, hierarchy, alert rule/event | AC-09, AC-10, AC-23, AC-27 |
| **W5 — Dashboard + facts** | Ảnh 19, compute batch, drill-down, footer | AC-02, AC-03, AC-04, AC-18, AC-26 |
| **W6 — Quality + Reports + Settings** | Ảnh 18, 20, 21 | AC-11…AC-14, AC-28, AC-29 |
| **W7 — Ship** | Guard, seed RBAC, E2E visual, deploy script, user guide | AC-15, AC-17, AC-19, AC-20 |

P2 (sau plan này): forecast, Power BI live, Slack, SSO, composite nâng cao. Không mở trong W0–W7 trừ export XLSX Dictionary/Target.

---

## File map

| File | Vai trò | Sóng |
|---|---|---|
| `docs/superpowers/specs/2026-09-04-kpi-hub-srs.md` | Spec v1.1 (đã có) | — |
| `docs/specs/2026-09-04-postgresql-ddl-kpi-hub.sql` | DDL + seed workspace + 21 KPI | W1 |
| `scripts/apply_pg_ddl_kpi_hub.sh` | Apply DDL | W1 |
| `scripts/seed_kpi_hub_rbac.sh` | Cap SUPER-ADMIN / CEO / GD | W7 |
| `scripts/deploy_kpi_hub_vps.sh` | DDL + jest + vitest + build | W7 |
| `services/ptt-crm-api/src/kpi-hub/kpi-hub.types.ts` | Enums, DTO, error codes | W1 |
| `services/ptt-crm-api/src/kpi-hub/kpi-hub-status.ts` | `deriveHubStatus`, `achievementPct`, `ratioPeriod` | W1 |
| `services/ptt-crm-api/src/kpi-hub/kpi-hub-status.spec.ts` | Jest | W1 |
| `services/ptt-crm-api/src/kpi-hub/kpi-hub.module.ts` | Nest module | W1 |
| `services/ptt-crm-api/src/kpi-hub/workspace/*` | GET/PATCH workspace | W1, W6 |
| `services/ptt-crm-api/src/kpi-hub/dictionary/*` | CRUD publish version | W2 |
| `services/ptt-crm-api/src/kpi-hub/formula/*` | AST, cycle, DIVIDE | W3 |
| `services/ptt-crm-api/src/kpi-hub/mapping/*` | Bindings, lineage, validate | W3 |
| `services/ptt-crm-api/src/kpi-hub/targets/*` | Period target + hierarchy | W4 |
| `services/ptt-crm-api/src/kpi-hub/alerts/*` | Rules + events + dedup | W4 |
| `services/ptt-crm-api/src/kpi-hub/facts/*` | Batch compute | W5 |
| `services/ptt-crm-api/src/kpi-hub/dashboard/*` | GET /dashboard | W5 |
| `services/ptt-crm-api/src/kpi-hub/quality/*` | Rules, runs, issues | W6 |
| `services/ptt-crm-api/src/kpi-hub/reports/*` | Library, schedule, send | W6 |
| `services/ptt-crm-api/src/app.module.ts` | Import `KpiHubModule` | W1 |
| `services/ptt-crm-api/src/staff-permissions/rbac-admin-catalog.json` | 7 section Hub | W0/W7 |
| `services/ops-web/src/lib/kpi-hub-api.ts` | Client | W2+ |
| `services/ops-web/src/lib/kpi-hub-status.ts` | Copy cùng logic status (vitest) | W1 |
| `services/ops-web/src/lib/kpi-hub-fixtures.ts` | Fixture 09/2026 = mockup | W0 |
| `services/ops-web/src/components/kpi-hub/KpiHubShell.tsx` | Sidebar + header + footer | W0 |
| `services/ops-web/src/components/kpi-hub/KpiHubSidebar.tsx` | 7 mục + Thu gọn | W0 |
| `services/ops-web/src/components/kpi-hub/KpiHubFreshnessFooter.tsx` | Ribbon | W0 |
| `services/ops-web/src/app/crm/kpi-hub/**` | 8 route | W0–W6 |
| `services/ops-web/src/app/globals.css` | `.kpi-hub-*` | W0 |
| `services/ops-web/src/lib/rbac-routes.ts` | Prefix `/crm/kpi-hub` | W0 |
| `services/ops-web/src/lib/admin/admin-nav.ts` | Link Thiết lập KPI → KPI Hub | W0 |
| `services/ops-web/src/components/OpsNav.tsx` | Item KPI Hub | W0 |
| `services/ops-web/e2e/kpi-hub.spec.ts` | Playwright chrome + 8 URL | W7 |
| `docs/huong-dan-su-dung/33-kpi-hub.md` | User guide | W7 |

---

## Schema PostgreSQL (W1)

Tạo `docs/specs/2026-09-04-postgresql-ddl-kpi-hub.sql` idempotent. Cột chung mọi bảng nghiệp vụ: `id uuid`, `tenant_id text NOT NULL DEFAULT 'PTT'`, `workspace_id uuid`, `created_at`, `updated_at`, `deleted_at`, `row_version int DEFAULT 1`.

Bảng tối thiểu P1:

```sql
-- workspace mặc định 1 dòng
crm_kpi_hub_workspaces
crm_kpi_dictionary
crm_kpi_dictionary_versions
crm_kpi_formula_parts
crm_kpi_source_connections
crm_kpi_source_bindings
crm_kpi_mapping_rules
crm_kpi_period_targets
crm_kpi_alert_rules
crm_kpi_alert_events
crm_kpi_facts
crm_kpi_quality_rules
crm_kpi_quality_runs
crm_kpi_quality_issues
crm_kpi_reports
crm_kpi_report_schedules
crm_kpi_report_shares
crm_kpi_hub_audit_logs
```

Unique: `(workspace_id, code)` dictionary WHERE deleted_at IS NULL.  
Unique: `(dictionary_id, period_start, grain, scope_hash)` targets.  
Check status dictionary: `DRAFT|PENDING_APPROVAL|ACTIVE|NEED_REVIEW|DEPRECATED|ARCHIVED`.

Seed W1: 1 workspace `KPI Hub - Marketing & Sales`, connections CRM/Meta/SharePoint/ERP/GA4 (GA4 có thể UNAVAILABLE), 21 KPI mục 29 spec (Draft rồi publish `MKT_006` + upstream trong W2).

---

## API (prefix `/api/crm/kpi-hub`)

Guard class `StaffKpiHubGuard` đọc section từ decorator. Controller mỏng, service chứa BR.

| Method | Path | Sóng |
|---|---|---|
| GET/PATCH | `/workspace` | W1 / W6 |
| GET/POST | `/dictionary` | W2 |
| GET/PATCH | `/dictionary/:id` | W2 |
| POST | `/dictionary/:id/publish` | W2 |
| POST | `/dictionary/:id/validate` | W3 |
| GET | `/sources` + POST refresh | W3 |
| GET/POST/PATCH | `/targets` | W4 |
| GET | `/alerts` + POST ack | W4 |
| GET | `/dashboard` | W5 |
| GET | `/quality` + POST run | W6 |
| GET/POST | `/reports` + share/schedule/send | W6 |
| GET | `/activity` | W6 |

---

## Domain thuần (W1) — viết test trước

**Files:**
- Create: `services/ptt-crm-api/src/kpi-hub/kpi-hub-status.ts`
- Test: `services/ptt-crm-api/src/kpi-hub/kpi-hub-status.spec.ts`
- Create: `services/ops-web/src/lib/kpi-hub-status.ts` (cùng hàm, vitest `kpi-hub-status.spec.ts`)

**Produces:**

```ts
export type HubDirection = 'HIGHER_IS_BETTER' | 'LOWER_IS_BETTER' | 'RANGE' | 'NEUTRAL';
export type HubPerfStatus = 'ACHIEVED' | 'WARNING' | 'CRITICAL' | 'NO_DATA' | 'NO_STATUS';

export function deriveHubStatus(input: {
  direction: HubDirection;
  actual: number | null;
  target: number | null;
  warning: number | null;
  critical: number | null;
}): HubPerfStatus;

export function achievementPct(
  direction: HubDirection,
  actual: number | null,
  target: number | null,
): number | null;

export function ratioPeriod(
  num: number,
  den: number,
  blankIfZero: boolean,
): number | null;

export function freshnessStatus(
  lastSuccessAt: Date | null,
  slaMinutes: number,
  failed: boolean,
  now: Date,
): 'FRESH' | 'DELAYED' | 'FAILED' | 'UNKNOWN';

export function hasFormulaCycle(
  edges: Array<{ from: string; to: string }>,
): boolean;
```

- [ ] **Step 1: Viết Jest fail**

```ts
// kpi-hub-status.spec.ts
it('CPL 142k vs target 150k warn 180k crit 220k → ACHIEVED', () => {
  expect(deriveHubStatus({
    direction: 'LOWER_IS_BETTER',
    actual: 142000, target: 150000, warning: 180000, critical: 220000,
  })).toBe('ACHIEVED');
});
it('MQL 24.8 vs target 30 → WARNING', () => {
  expect(deriveHubStatus({
    direction: 'HIGHER_IS_BETTER',
    actual: 24.8, target: 30, warning: null, critical: 20,
  })).toBe('WARNING');
});
it('Win 12.5 vs target 20 crit 20 → CRITICAL', () => {
  expect(deriveHubStatus({
    direction: 'HIGHER_IS_BETTER',
    actual: 12.5, target: 20, warning: null, critical: 20,
  })).toBe('CRITICAL');
});
it('ratio blank-if-zero', () => {
  expect(ratioPeriod(100, 0, true)).toBeNull();
});
it('non-additive: không avg tỷ lệ ngày', () => {
  expect(ratioPeriod(300, 2, false)).toBe(150);
});
it('cycle A→B→A', () => {
  expect(hasFormulaCycle([{ from: 'A', to: 'B' }, { from: 'B', to: 'A' }])).toBe(true);
});
it('SharePoint trễ > SLA → DELAYED', () => {
  expect(freshnessStatus(new Date('2026-09-04T06:30:00+07:00'), 60, false, new Date('2026-09-04T08:45:00+07:00'))).toBe('DELAYED');
});
```

- [ ] **Step 2:** `cd services/ptt-crm-api && npx jest src/kpi-hub/kpi-hub-status.spec.ts --runInBand` → FAIL (module thiếu)
- [ ] **Step 3:** Implement đúng SRS FR-TGT-07 / BR-06 / BR-07
- [ ] **Step 4:** Jest PASS; copy sang ops-web vitest
- [ ] **Step 5:** Commit `feat(kpi-hub): add status/ratio/freshness domain`

---

### Task 0: Shell UI đúng mockup (W0)

**Files:**
- Create: `services/ops-web/src/components/kpi-hub/KpiHubShell.tsx`
- Create: `services/ops-web/src/components/kpi-hub/KpiHubSidebar.tsx`
- Create: `services/ops-web/src/components/kpi-hub/KpiHubFreshnessFooter.tsx`
- Create: `services/ops-web/src/lib/kpi-hub-nav.ts`
- Create: `services/ops-web/src/lib/kpi-hub-nav.spec.ts`
- Create: `services/ops-web/src/app/crm/kpi-hub/page.tsx` (Dashboard placeholder)
- Create: `services/ops-web/src/app/crm/kpi-hub/dictionary/page.tsx`
- Create: `services/ops-web/src/app/crm/kpi-hub/targets/page.tsx`
- Create: `services/ops-web/src/app/crm/kpi-hub/sources/page.tsx`
- Create: `services/ops-web/src/app/crm/kpi-hub/quality/page.tsx`
- Create: `services/ops-web/src/app/crm/kpi-hub/reports/page.tsx`
- Create: `services/ops-web/src/app/crm/kpi-hub/settings/page.tsx`
- Modify: `services/ops-web/src/app/globals.css` (block `.kpi-hub-*`)
- Modify: `services/ops-web/src/components/OpsNav.tsx` — thêm `{ href: '/crm/kpi-hub', label: 'KPI Hub' }` cạnh KPI
- Modify: `services/ops-web/src/lib/admin/admin-nav.ts` — `buildKpiSetupLinks` thêm Hub khi `crm_kpi_hub` view
- Modify: `services/ops-web/src/lib/admin/admin-nav.spec.ts`
- Modify: `services/ops-web/src/lib/rbac-routes.ts` — prefix `/crm/kpi-hub` → `crm_kpi_hub` view
- Modify: `services/ptt-crm-api/src/staff-permissions/rbac-admin-catalog.json` — section `crm_kpi_hub` (`view`) tạm; đủ 7 section ở W7

**Produces:**

```ts
export const KPI_HUB_NAV = [
  { href: '/crm/kpi-hub', label: 'Dashboard', icon: 'dashboard' },
  { href: '/crm/kpi-hub/dictionary', label: 'KPI Dictionary', icon: 'book' },
  { href: '/crm/kpi-hub/targets', label: 'Target & Cảnh báo', icon: 'target' },
  { href: '/crm/kpi-hub/sources', label: 'Nguồn dữ liệu', icon: 'database' },
  { href: '/crm/kpi-hub/quality', label: 'Data Quality', icon: 'shield' },
  { href: '/crm/kpi-hub/reports', label: 'Báo cáo', icon: 'chart' },
  { href: '/crm/kpi-hub/settings', label: 'Cài đặt', icon: 'gear' },
] as const;
```

`KpiHubShell` props: `title`, `subtitle`, `breadcrumb: {label, href?}[]`, `actions?: ReactNode`, `showFreshness?: boolean`, `children`.  
**Không** render `OpsNav` sidebar bên trong trang Hub. Dùng layout tối giản (header user từ session, không clone topbar OpsNav đầy đủ — mockup chỉ bell + avatar `PTT`).

- [ ] **Step 1:** Vitest `kpi-hub-nav.spec.ts` — 7 label đúng thứ tự
- [ ] **Step 2:** Run fail
- [ ] **Step 3:** Implement nav + Shell + 7 page bọc Shell, trang body `Đang tải…` tạm
- [ ] **Step 4:** CSS sidebar 240px, active xanh lá nhạt, nút Thu gọn đổi class `kpi-hub-shell--collapsed`
- [ ] **Step 5:** So ảnh 19: sidebar + header. Sai nhãn = sửa ngay
- [ ] **Step 6:** Commit `feat(kpi-hub): add shell sidebar matching mockup`

---

### Task 1: DDL + workspace API (W1)

**Files:**
- Create: `docs/specs/2026-09-04-postgresql-ddl-kpi-hub.sql`
- Create: `scripts/apply_pg_ddl_kpi_hub.sh`
- Create: `services/ptt-crm-api/src/kpi-hub/kpi-hub.types.ts` — error codes SRS §17
- Create: `services/ptt-crm-api/src/kpi-hub/workspace/kpi-hub-workspace.repository.ts`
- Create: `services/ptt-crm-api/src/kpi-hub/workspace/kpi-hub-workspace.service.ts`
- Create: `services/ptt-crm-api/src/kpi-hub/kpi-hub.controller.ts` — GET/PATCH `/workspace`
- Create: `services/ptt-crm-api/src/kpi-hub/guards/staff-kpi-hub.guard.ts`
- Modify: `app.module.ts`

**Interfaces:**
- Consumes: `kpi-hub-status.ts`
- Produces: `GET /api/crm/kpi-hub/workspace` → `{ id, name, company, timezone, locale, currency, week_start, default_period_grain, close_day, reconcile_day, ... }`

- [ ] **Step 1:** Jest service: PATCH close_day 3 / reconcile_day 5 lưu được; tenant khác 404
- [ ] **Step 2:** Fail
- [ ] **Step 3:** DDL + seed workspace PTT Digital / Asia/Ho_Chi_Minh
- [ ] **Step 4:** Apply local + Jest pass
- [ ] **Step 5:** Commit `feat(kpi-hub): add workspace DDL and API`

---

### Task 2: Dictionary list + drawer đúng ảnh 14 (W2)

**Files:**
- Create: `services/ops-web/src/components/kpi-hub/dictionary/KpiHubDictSummaryCards.tsx`
- Create: `services/ops-web/src/components/kpi-hub/dictionary/KpiHubDictFilterBar.tsx`
- Create: `services/ops-web/src/components/kpi-hub/dictionary/KpiHubDictTable.tsx`
- Create: `services/ops-web/src/components/kpi-hub/dictionary/KpiHubDictDrawer.tsx`
- Create: `services/ops-web/src/lib/kpi-hub-fixtures.ts` — 22 KPI mockup (gồm MKT_006)
- Modify: `app/crm/kpi-hub/dictionary/page.tsx`
- Create: API `dictionary.controller.ts` + repository + `kpi-hub-api.ts`

**UI bắt buộc:** 4 thẻ 22/20/1/7 khi seed demo; search placeholder đúng; badge nhóm Acquisition xanh dương, Media Efficiency tím, Funnel cam, Sales Outcome xanh lá, Unit Economics đỏ; drawer CPL phân số + `≤ 150.000 VND` + `Chỉnh sửa KPI`.

- [ ] **Step 1:** Vitest fixture `MKT_006` name/group/formula
- [ ] **Step 2:** Trang list render fixture (chưa API) — so ảnh 14
- [ ] **Step 3:** API list + summary; thay fixture khi `?live=1` hoặc env
- [ ] **Step 4:** Jest list tenant isolation
- [ ] **Step 5:** Commit `feat(kpi-hub): dictionary list and drawer`

---

### Task 3: Editor 5 tab + publish (W2)

**Files:**
- Create: `app/crm/kpi-hub/dictionary/new/page.tsx`
- Create: `app/crm/kpi-hub/dictionary/[id]/edit/page.tsx`
- Create: `components/kpi-hub/dictionary/KpiHubEditorChrome.tsx` — title, badge Active, 2 nút, 5 tab
- Create: `components/kpi-hub/dictionary/tabs/OverviewTab.tsx`
- Create: `components/kpi-hub/dictionary/tabs/GovernanceTab.tsx`
- Create: placeholder tabs Formula/Source/Target (điền W3/W4)

**Copy chrome ảnh 16:** `Lưu nháp` trắng · `Lưu & Xuất bản` xanh. Tab query `?tab=overview|formula|source|target|governance`.

Publish API: chặn thiếu formula/source/time basis/owner → `KPI_HUB_PUBLISH_INCOMPLETE`. Active sửa formula → version mới.

- [ ] **Step 1:** Jest publish incomplete
- [ ] **Step 2:** UI chrome so ảnh 16 (header + tab), tab Tổng quan form
- [ ] **Step 3:** Wire save draft / publish
- [ ] **Step 4:** Commit `feat(kpi-hub): dictionary editor chrome and publish`

---

### Task 4: Tab Công thức đúng ảnh 16 (W3)

**Files:**
- Create: `components/kpi-hub/formula/KpiHubFormulaTab.tsx`
- Create: `components/kpi-hub/formula/FormulaPartCard.tsx`
- Create: `components/kpi-hub/formula/FormulaPreview.tsx`
- Create: `components/kpi-hub/formula/FormulaSidebar.tsx`
- Create: `ptt-crm-api/src/kpi-hub/formula/kpi-hub-formula.parser.ts` + spec
- Create: `POST /dictionary/:id/validate`

**UI:** Ratio dropdown; tử số `Tổng chi tiêu quảng cáo` `MKT_004` `SUM(AdInsights[Spend])` filter chip; mẫu số `MKT_002`; ô xanh `CPL Valid Lead = … ÷ …`; DAX `DIVIDE([Tổng chi tiêu quảng cáo], [Tổng Valid Leads])`; 3 toggle; sidebar time/check/deps `MKT_004` `MKT_002` → `MKT_009` `FIN_003`.

- [ ] **Step 1:** Jest cycle + DIVIDE preview string
- [ ] **Step 2:** Render tab bằng fixture CPL — so ảnh 16 pixel khối
- [ ] **Step 3:** Validate API
- [ ] **Step 4:** Commit `feat(kpi-hub): formula tab and parser`

---

### Task 5: Tab Mapping + catalog nguồn (W3)

**Files:**
- Create: `components/kpi-hub/mapping/KpiHubMappingTab.tsx`
- Create: `components/kpi-hub/mapping/SourceBindingTable.tsx`
- Create: `components/kpi-hub/mapping/MappingDetailCard.tsx`
- Create: `components/kpi-hub/mapping/MappingRulesRail.tsx`
- Create: `app/crm/kpi-hub/sources/page.tsx` — catalog (sidebar mục 4)
- Create: mapping repository + validate (unmapped count, n-n warning)

**UI ảnh 17:** 3 hàng nguồn + 2 card + rail 96% + `12 campaign chưa được mapping` + `Mở Data Quality`.

- [ ] **Step 1:** Jest validate missing join key → `KPI_HUB_JOIN_KEY_MISSING`
- [ ] **Step 2:** UI so ảnh 17
- [ ] **Step 3:** Catalog `/sources` freshness
- [ ] **Step 4:** Commit `feat(kpi-hub): mapping tab and source catalog`

---

### Task 6: Target & cảnh báo đúng ảnh 15 (W4)

**Files:**
- Create: `components/kpi-hub/targets/KpiHubTargetCards.tsx`
- Create: `components/kpi-hub/targets/KpiHubTargetTable.tsx`
- Create: `components/kpi-hub/targets/KpiHubTargetDrawer.tsx`
- Modify: `app/crm/kpi-hub/targets/page.tsx`
- Create: `targets.service.ts` — hierarchy Campaign|User > Team > Dept > Workspace
- Create: `alerts.service.ts` — dedup window, recovery

**UI:** 4 thẻ 18/22 12 4 2; bảng CPL Đạt / MQL Cảnh báo / Win Nguy cấp / Response Cảnh báo; drawer gauge + Email/Teams.

- [ ] **Step 1:** Jest hierarchy + dedup không gửi 2 lần cùng level
- [ ] **Step 2:** UI fixture so ảnh 15
- [ ] **Step 3:** API upsert + If-Match
- [ ] **Step 4:** Commit `feat(kpi-hub): targets and alerts`

---

### Task 7: Dashboard đúng ảnh 19 + facts (W5)

**Files:**
- Create: `components/kpi-hub/dashboard/KpiHubDashCards.tsx`
- Create: `components/kpi-hub/dashboard/KpiHubFunnel.tsx`
- Create: `components/kpi-hub/dashboard/KpiHubTargetDonut.tsx`
- Create: `components/kpi-hub/dashboard/KpiHubChannelChart.tsx`
- Create: `components/kpi-hub/dashboard/KpiHubAlertList.tsx`
- Create: `components/kpi-hub/dashboard/KpiHubTopSales.tsx`
- Create: `components/kpi-hub/dashboard/KpiHubDrilldown.tsx`
- Modify: `app/crm/kpi-hub/page.tsx`
- Create: `facts/kpi-hub-facts.service.ts` — cron/batch
- Create: `dashboard/kpi-hub-dashboard.service.ts`

**Thứ tự khối trên trang = ảnh 19, không đảo.** Chart: CSS/SVG nội bộ hoặc Recharts nếu repo đã có; không thêm lib nặng nếu chưa có. Funnel = HTML/CSS bar ngang như mockup.

Compute: `ratioPeriod` + bindings CRM/Meta. `SAL_008` ≠ `FIN_001`. Failed source → card `DATA_ISSUE`.

- [ ] **Step 1:** Jest dashboard mapper fixture 09/2026 (5 thẻ + bottleneck `MKT_008`)
- [ ] **Step 2:** UI fixture so ảnh 19 toàn trang
- [ ] **Step 3:** `GET /dashboard` + footer freshness
- [ ] **Step 4:** Commit `feat(kpi-hub): dashboard and fact compute`

---

### Task 8: Data Quality đúng ảnh 18 (W6)

**Files:**
- Create: `components/kpi-hub/quality/*` (score, trend, freshness, table, issue drawer)
- Modify: `quality/page.tsx`
- Create: quality service + default rules SRS FR-DQ-07

**UI:** gauge 92, tabs 14/1/3/10, drawer Lead_ID, `Lead_ID IS NOT NULL`.

- [ ] **Step 1:** Jest completeness count
- [ ] **Step 2:** UI so ảnh 18
- [ ] **Step 3:** run + assign + ticket nội bộ
- [ ] **Step 4:** Commit `feat(kpi-hub): data quality`

---

### Task 9: Báo cáo đúng ảnh 20 (W6)

**Files:**
- Create: `components/kpi-hub/reports/*`
- Modify: `reports/page.tsx`
- Create: reports service (create from 4 quick templates, schedule, send stub email)

**UI:** 4 thẻ 12/6/2/28; tab 4; quick create 4 ô; lịch gửi; chia sẻ gần đây; footer freshness.

- [ ] **Step 1:** Jest không send khi KPI bắt buộc Failed → `KPI_HUB_REPORT_SOURCE_FAILED`
- [ ] **Step 2:** UI so ảnh 20
- [ ] **Step 3:** Commit `feat(kpi-hub): reports library`

---

### Task 10: Cài đặt đúng ảnh 21 (W6)

**Files:**
- Create: `components/kpi-hub/settings/KpiHubSettingsNav.tsx` — 9 mục
- Create: `components/kpi-hub/settings/WorkspacePanel.tsx`
- Create: `components/kpi-hub/settings/SystemStatusRail.tsx`
- Create: `components/kpi-hub/settings/QuickToggles.tsx`
- Modify: `settings/page.tsx`
- Extend PATCH workspace

**UI:** 9 mục con; form org; chip Tháng; danger zone; rail CRM/Meta/GA4/SharePoint/ERP; 4 toggle.

- [ ] **Step 1:** Jest maintenance → ghi 503 `KPI_HUB_MAINTENANCE` trừ Super Admin
- [ ] **Step 2:** UI so ảnh 21
- [ ] **Step 3:** Commit `feat(kpi-hub): settings workspace`

---

### Task 11: RBAC đủ 7 section + seed + E2E + deploy (W7)

**Files:**
- Modify: `rbac-admin-catalog.json` — `crm_kpi_hub`, `crm_kpi_dictionary`, `crm_kpi_hub_targets`, `crm_kpi_hub_sources`, `crm_kpi_quality`, `crm_kpi_hub_reports`, `crm_kpi_hub_settings` + actions SRS §21
- Modify: `rbac-routes.ts` — prefix cụ thể dài hơn trước `/crm/kpi-hub`
- Create: `scripts/seed_kpi_hub_rbac.sh`
- Create: `scripts/deploy_kpi_hub_vps.sh`
- Create: `e2e/kpi-hub.spec.ts` — login, mở 8 URL, sidebar 7 text, Dashboard title, Dictionary `+ Tạo KPI`, Target title, Settings `Không gian làm việc`
- Create: `docs/huong-dan-su-dung/33-kpi-hub.md`
- Modify: `admin-nav.spec.ts`

- [ ] **Step 1:** Jest/Vitest nav + catalog id
- [ ] **Step 2:** Playwright AC-01
- [ ] **Step 3:** Deploy script: apply DDL, seed RBAC, jest kpi-hub, vitest kpi-hub, `next build` ops-web
- [ ] **Step 4:** Commit `feat(kpi-hub): rbac e2e and deploy`

---

## Thứ tự phụ thuộc

```text
W0 Shell
  → W1 Domain + DDL + workspace
    → W2 Dictionary UI/API
      → W3 Formula + Mapping
        → W4 Target + Alert
          → W5 Dashboard + Facts
            → W6 Quality + Reports + Settings
              → W7 RBAC / E2E / deploy
```

Không làm W5 trước W4 (thẻ Dashboard cần status target). Không làm W3 trước chrome editor W2.

---

## Kiểm tra plan vs spec

| Spec | Task |
|---|---|
| Shell 7 mục, footer freshness | T0, T7, T9 |
| Dictionary + 5 tab + publish/version | T2, T3 |
| Công thức Ratio, blank-if-zero, cycle | T4, W1 domain |
| Mapping 3 nguồn, UTM, 12 campaign | T5 |
| Target hierarchy, alert 4h | T6 |
| Dashboard 5 thẻ, funnel, donut, Top Sales | T7 |
| Data Issue precedence | W1 + T7 |
| Quality 92 + Lead_ID | T8 |
| Báo cáo library + lịch | T9 |
| Settings workspace + danger | T10 |
| Catalog 21 KPI | DDL seed T1/T2 |
| Power BI live / AI / SSO | **Ngoài W0–W7** (P2+) |
| Gắn KPI Type Inactive → Need Review | T3 + T11 AC-20 |
| Tenant isolation, 409 | T1, T6 |

---

## Ghi chú implementer

- Mỗi task UI: mở ảnh mockup tương ứng **trước khi code**, dựng HTML/CSS, rồi mới fetch.
- Reuse `hasCap`, `staffMe`, `HubPageLayout` chỉ nếu không phá sidebar Hub. Ưu tiên `KpiHubShell` full viewport dưới top bar app nếu bắt buộc giữ login chrome tối thiểu.
- Chart: ưu tiên CSS; nếu repo đã có `recharts` thì dùng, không thêm lib mới nếu không cần.
- Tiếng Việt trên UI copy từng chữ mockup (`Thu gọn`, `Chỉnh sửa KPI`, `Lưu & Xuất bản`, `Chạy kiểm tra`).
- Không commit secret. Không `--no-verify`.

---

*Plan bám SRS v1.1 + 8 mockup. Xong W7 = Hub dùng được nội bộ; P2 mở Power BI/forecast/SSO.*
