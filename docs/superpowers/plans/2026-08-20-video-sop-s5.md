# Video SOP Module 7 — Sprint 5 (Gate 1–2 + SC-10 UI) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** S5 mở **Gate 1–2** (FR-7.7, BR-04, AC-R3) với approve/reject/override + **SC-10** UI — không Kling, không Gate 3–4 logic UI, không import `video-social`.

**Architecture:** Nest `video-sop/gate` + mở rộng `vd-stage.guard` + shotlist lock. HTTP `/api/v1/vd/projects/:id/gates/:n` · `/shotlist/ready` · `/stage` · `/shots/:id/approve-keyframe`.

**Parent:** [`2026-08-20-video-sop-module-7.md`](./2026-08-20-video-sop-module-7.md) Tasks 23–25.

## Global Constraints

- **Không** import `video-social` / `social-ffmpeg`.
- **Không** Kling/Runway (S6). **Không** Gate 3–4 approve UI (S8).
- Flag cinematic bắt buộc.
- **UI bắt buộc:** SC-10 `/crm/video/[id]/gates/[n]` (n=1,2).
- Caps hand-add: `crm_vd.gate1`, `crm_vd.gate2` (view + approve). **Do not** run export script.
- Gate 1 approve: `assertBriefComplete` + `assertFeasibilityPass` + ≥1 shot → stage `keyframing`, lock shotlist (`shotlist_immutable`).
- Gate 2 approve: bible exists + mọi shot `keyframe_approved` → stage `animating`.
- Override: `override_reason.trim().length >= 10` + cap gate approve **or** `crm_vd.project` edit.
- Reject → `vd_rework_items`, không đổi stage.
- Stage guard: `shotlist_ready→keyframing` cần gate1 approved; `keyframing→animating` cần gate2 approved (AC-R3).

---

### Task 1: DDL + StageGuard

**Files:** `postgresql-ddl-vd-sop-s5.sql`, `apply_pg_ddl_vd_sop_s5.sh`, `vd-stage.guard.ts` + spec

**Tables:** `vd_gates`, `vd_approvals`, `vd_rework_items`

- [ ] Test AC-R3 gate2 pending blocks animating
- [ ] Implement DDL + extended stage chain
- [ ] `feat(vd): stage guard requires gates`

### Task 2: Gate API + SC-10

**Files:** `gate/vd-gate.*`, guards, `gates/[n]/page.tsx`, `video-sop-api.ts`, hub links

HTTP:
- `GET /projects/:id/gates/:n` → `{ gate_no, status, checklist: [{ id, label, ok }] }`
- `POST .../approve` `{ override?: boolean, override_reason?: string }`
- `POST .../reject` `{ reason: string }`
- `POST /projects/:id/shotlist/ready` — scripting → shotlist_ready
- `POST /projects/:id/stage` `{ stage }` — smoke AC-R3
- `POST /shots/:id/approve-keyframe` — keyframe_pending → keyframe_approved

SC-10 UI strings:
- Title: `Gate Review (SC-10)`
- Banner G1: `S5 — Gate 1 shotlist. BR-04 immutable sau approve.`
- Banner G2: `S5 — Gate 2 keyframe. AC-R3 animating.`
- Buttons: `Approve` · `Reject` · `Override`

- [ ] `feat(vd): gate 1-2 approve reject override SC-10`

### Task 3: Smoke S5

- [ ] `scripts/smoke_video_sop_s5.sh` — stage animating blocked; G1+G2 → keyframing → animating
- [ ] `test(vd): S5 smoke AC-R3 stage guard`
