# Video SOP Module 7 — Sprint 3 (Brief + Script + feasibility + UI) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** S3 mở **Brief 8 nhóm (BR-01)** + **Script Studio** + **feasibility FR-R01…10** và **UI thật**: SC-03 `/crm/video/[id]/brief` + SC-04 `/crm/video/[id]/script` — không Gate, không Bible, không Kling, không import `video-social`.

**Architecture:** Nest `video-sop/project` (brief) + `script` + `rules/vd-feasibility` + `adapters/i-text-gen`. HTTP `/api/v1/vd/projects/:id/brief` · `/ideas` · `/scripts` · `/scripts/:id/shots`. UI 8 field + insight picker + 3 cột template | ý tưởng | shotlist.

**Tech Stack:** NestJS `ptt-crm-api`, Next.js `ops-web`, PostgreSQL `vd_ideas`/`vd_shots`/`vd_prompts`/`vd_prompt_templates` (S1 đã có `vd_briefs`/`vd_scripts`), Jest, Vitest, RBAC `crm_vd.project` + `crm_vd.script`.

**Spec:** [`2026-08-20-video-sop-module-7-design.md`](../specs/2026-08-20-video-sop-module-7-design.md) §7 BR-01/BR-02 · §11 SC-03/SC-04 · S3.

**Parent:** [`2026-08-20-video-sop-module-7.md`](./2026-08-20-video-sop-module-7.md) Tasks 15–19.

## Global Constraints

- **Không** import `content-marketing/video-social/**`, `social-ffmpeg`, `social-video.service`, `content-media-image.provider`. `social-studio.util` vẫn được.
- **Không** job `social_*`. S3 text job chỉ `cine_director` trên `q.text`.
- **Không** Gate 1–4, Bible, keyframe workbench, Kling, cost, portal (S4+).
- Secret text = env `OPENAI_API_KEY`. Thiếu key → stub 3 idea cố định, **không** tạo ảnh.
- HTTP generate ideas: `201 { id, status: 'queued' }` — không await OpenAI. UI poll `GET ideas`.
- Brief `insight_ids` luôn là `number[]`; được `[]` (BR-01 không bắt buộc ≥1).
- **Không** import `market-research/**` vào `video-sop`. Insight picker: SQL optional trên `crm_research_insights`; bảng thiếu → `[]`.
- Stage S3 allowed: `brief_draft→brief_ready`, `brief_ready→ideation`, `brief_ready→scripting`, `ideation→scripting`, và `from===to`. Mọi nhảy khác (kể cả `keyframing`/`animating`) → `stage_guard`.
- Caps: brief PUT/ready = `crm_vd.project` edit **hoặc** `crm_content` write. Ideas/script/shots = `crm_vd.script` edit **hoặc** `crm_vd.project` edit **hoặc** `crm_content` write. **Do not** run `export_rbac_admin_catalog.py`.
- Flag cinematic bắt buộc (`assertCinematicEnabled`) trên mọi POST/PUT S3.
- **UI bắt buộc:** SC-03 + SC-04. Không ship S3 chỉ API.
- Shot POST một shot: chỉ rule per-shot (FR-R01,R02,R03,R04,R06,R07,R08). Batch `{ shots: [...] }`: full FR-R01…10 kể cả R05/R09/R10.

---

## File map

```
docs/specs/postgresql-ddl-vd-sop-s3.sql
scripts/apply_pg_ddl_vd_sop_s3.sh
scripts/smoke_video_sop_s3.sh
docs/huong-dan-su-dung/19-video-sop.md

services/ptt-crm-api/src/video-sop/
  project/vd-brief.service.ts
  project/vd-brief.service.spec.ts
  project/vd-brief.controller.ts
  script/vd-script.service.ts
  script/vd-script.service.spec.ts
  script/vd-script.controller.ts
  script/vd-idea.repository.ts
  script/vd-shot.repository.ts
  adapters/i-text-gen.ts
  adapters/openai.text.ts
  adapters/i-text-gen.spec.ts
  rules/vd-feasibility.rules.ts
  rules/vd-feasibility.rules.spec.ts
  rules/vd-stage.guard.ts          # modify: S3 transitions
  rules/vd-stage.guard.spec.ts     # modify
  orchestration/vd-dispatcher.service.ts  # + cine_director
  video-sop.module.ts
  video-sop.types.ts               # + brief/idea/shot types if needed

Modify:
  staff-permissions/rbac-admin-catalog.json   # crm_vd.script
  admin_page_permissions.py

services/ops-web/src/
  lib/video-sop-api.ts + .spec.ts
  app/crm/video/[id]/brief/page.tsx     # SC-03
  app/crm/video/[id]/script/page.tsx    # SC-04
  app/crm/video/[id]/page.tsx           # links + S3 banner
```

**Ngoài S3:** SC-05/06 bible+keyframes (S4), Gate SC-10 (S5), Kling, cost, portal.

---

### Task 1: DDL ideas / shots / prompts / templates

**Files:**
- Create: `docs/specs/postgresql-ddl-vd-sop-s3.sql`
- Create: `scripts/apply_pg_ddl_vd_sop_s3.sh` (copy S2 script; print `DATABASE_URL=set` only)

**Interfaces:**

```sql
-- vd_ideas
id bigserial PK
project_id bigint NOT NULL REFERENCES vd_projects(id)
ordinal int NOT NULL CHECK (ordinal BETWEEN 1 AND 3)
summary text NOT NULL
selected boolean NOT NULL DEFAULT false
created_at timestamptz NOT NULL DEFAULT now()
UNIQUE (project_id, ordinal)

-- vd_shots
id bigserial PK
script_id bigint NOT NULL REFERENCES vd_scripts(id)
ordinal int NOT NULL
status text NOT NULL DEFAULT 'draft' CHECK (status IN (
  'draft','prompts_ready','keyframe_pending','keyframe_approved',
  'clip_draft','clip_final','clip_selected','posted','blocked','plan_b'
))
duration_ms int NOT NULL
camera text NOT NULL DEFAULT ''
action text NOT NULL DEFAULT ''
aspect text NOT NULL DEFAULT '9:16'
contains_human boolean NOT NULL DEFAULT false
text_in_frame boolean NOT NULL DEFAULT false
logo_in_ai_frame boolean NOT NULL DEFAULT false
seed bigint NULL
take_fail_count int NOT NULL DEFAULT 0
UNIQUE (script_id, ordinal)

-- vd_prompts
id bigserial PK
shot_id bigint NOT NULL REFERENCES vd_shots(id)
body text NOT NULL DEFAULT ''
bible_snapshot_json jsonb NOT NULL DEFAULT '{}'::jsonb
region_locked boolean NOT NULL DEFAULT false
created_at timestamptz NOT NULL DEFAULT now()

-- vd_prompt_templates
id bigserial PK
code text NOT NULL UNIQUE
kind text NOT NULL CHECK (kind IN ('brief','director','shot','keyframe','motion'))
body text NOT NULL DEFAULT ''
created_at timestamptz NOT NULL DEFAULT now()

-- Seed ON CONFLICT DO NOTHING:
INSERT (code, kind, body) VALUES
  ('vd.brief.v1', 'brief', 'Điền 8 nhóm SOP 1.1'),
  ('vd.director.v1', 'director', 'Sinh 3 ý tưởng video 15–60s'),
  ('vd.shot.v1', 'shot', 'Shot: camera + action, không chữ trong frame'),
  ('vd.keyframe.v1', 'keyframe', 'Keyframe cinematic, lock region bible'),
  ('vd.motion.v1', 'motion', 'Motion brief — S6');
```

- `vd_shots.shot_id` không tồn tại — jobs.shot_id vẫn không FK (S2).
- Script must not print the connection string.

- [ ] **Step 1:** Write SQL + apply script.

- [ ] **Step 2:** `bash -n scripts/apply_pg_ddl_vd_sop_s3.sh`. If no local `DATABASE_URL`, skip apply and report (same as S1/S2).

- [ ] **Step 3:** Seed 5 templates in the same SQL file.

- [ ] **Step 4:** Script prints `DATABASE_URL=set` only.

- [ ] **Step 5:** `feat(vd): S3 DDL ideas shots prompts`

---

### Task 2: BR-01 brief + SC-03 UI

**Files:**
- Create: `services/ptt-crm-api/src/video-sop/project/vd-brief.service.ts`
- Test: `services/ptt-crm-api/src/video-sop/project/vd-brief.service.spec.ts`
- Create: `services/ptt-crm-api/src/video-sop/project/vd-brief.controller.ts` (or add routes on `VdProjectController`)
- Modify: `services/ptt-crm-api/src/video-sop/rules/vd-stage.guard.ts` + `.spec.ts`
- Modify: `services/ptt-crm-api/src/video-sop/project/vd-project.repository.ts` — `getBrief` / `upsertBrief` / `updateStage`
- Modify: `video-sop.module.ts`
- Create: `services/ops-web/src/app/crm/video/[id]/brief/page.tsx`
- Modify: `services/ops-web/src/lib/video-sop-api.ts` + `.spec.ts`
- Modify: `services/ops-web/src/app/crm/video/[id]/page.tsx` — S3 banner + links
- Modify: `rbac-admin-catalog.json` + `admin_page_permissions.py` — add `crm_vd.script` view/edit, page `/crm/video`, group `CRM — Triển khai dịch vụ`. **Do not** run export script.

**Interfaces:**

```ts
export const BRIEF_KEYS = [
  'objective', 'audience', 'offer', 'duration_sec', 'platform', 'tone', 'constraints', 'insight_ids',
] as const;

export function assertBriefComplete(body: Record<string, unknown>): void;
// throws Error('brief_incomplete') when any of:
//   objective/audience/offer/constraints: trim length < 8
//   tone: trim length < 4
//   duration_sec: not number or not in 15..60
//   platform: not 'reels' | 'shorts' | 'feed_square'
//   insight_ids: missing or not an array of numbers (empty array OK)

export function assertStageTransition(from: VdProjectStage, to: VdProjectStage): void;
// allow from===to
// allow brief_draft→brief_ready, brief_ready→ideation, brief_ready→scripting, ideation→scripting
// else throw Error('stage_guard')
```

HTTP (flag on; cinematic assert on PUT/POST):
- `GET /api/v1/vd/projects/:id/brief` → `{ project_id, body_json, stage }`
- `PUT /api/v1/vd/projects/:id/brief` `{ ...8 keys }` — upsert `vd_briefs.body_json` (không đổi stage). Incomplete body **được lưu** (draft).
- `POST /api/v1/vd/projects/:id/brief/ready` — `assertBriefComplete` rồi `stage=brief_ready`. Incomplete → 400 `brief_incomplete`. Wrong from-stage → 400 `stage_guard`.
- `GET /api/v1/vd/projects/:id/brief/insights` → `{ items: { id: number, title: string }[] }`
  - SQL: `SELECT i.id, left(i.statement, 120) AS title FROM crm_research_insights i WHERE i.status IN ('approved_internal','approved_client_facing','published') ORDER BY i.id DESC LIMIT 50`
  - Table missing / query fail → `{ items: [] }` (không 500)
  - **Không** import `src/market-research/**`

Guard: GET brief/insights = project view (same as S1 view). PUT/ready = project edit **or** `crm_content` write.

Map errors to 400: `brief_incomplete`, `stage_guard`, `cmkt_cinematic_disabled`, `vd_tables_missing`, `invalid_body`. 404: `vd_project_not_found`.

SC-02 (`/crm/video/[id]`):
- Banner **exact**: `S3 — Brief + Script. Gate 1 vẫn S5.`
- Links **exact** text: `Brief (SC-03)` → `/crm/video/${id}/brief` · `Script (SC-04)` → `/crm/video/${id}/script`
- Keep S2 jobs table + enqueue button.

SC-03 (`/crm/video/[id]/brief`) — `CrmDeliveryPageShell`, auth/refresh như SC-02:
- Title: `Brief (SC-03)`
- Banner **exact**: `S3 — 8 nhóm SOP. Insight M6 được để trống.`
- Flag off → `Module tắt`
- Fields + labels **exact**:
  - `Mục tiêu` → `objective` (textarea)
  - `Đối tượng` → `audience`
  - `Offer` → `offer`
  - `Thời lượng (giây)` → `duration_sec` (number)
  - `Nền tảng` → `platform` (select: `reels` | `shorts` | `feed_square`)
  - `Tone` → `tone`
  - `Ràng buộc` → `constraints`
  - `Insights M6` → checkboxes from GET insights; empty copy **exact**: `Không có insight approved — được để trống.`
- Button **exact**: `Lưu brief` (PUT). Hidden unless project edit **or** content write.
- Button **exact**: `Đánh dấu brief sẵn sàng` (POST ready). Same cap hide.
- After ready 200: show `stage` from response.

Path helpers:

```ts
export function vdProjectBriefPath(id: number | string): string {
  return `/api/v1/vd/projects/${encodeURIComponent(String(id))}/brief`;
}
export function vdProjectBriefReadyPath(id: number | string): string {
  return `/api/v1/vd/projects/${encodeURIComponent(String(id))}/brief/ready`;
}
export function vdProjectBriefInsightsPath(id: number | string): string {
  return `/api/v1/vd/projects/${encodeURIComponent(String(id))}/brief/insights`;
}
```

- [ ] **Step 1: Tests**

```ts
it('fails when objective short', () => {
  expect(() => assertBriefComplete({
    objective: 'hi', audience: 'khách hàng phổ thông A', offer: 'gói retainer content',
    duration_sec: 30, platform: 'reels', tone: 'rõ ràng', constraints: 'không mặt người',
    insight_ids: [],
  })).toThrow(/brief_incomplete/);
});
it('passes with eight groups', () => {
  expect(() => assertBriefComplete({
    objective: 'tăng nhận biết', audience: 'khách hàng phổ thông A', offer: 'gói retainer content',
    duration_sec: 30, platform: 'reels', tone: 'rõ ràng', constraints: 'không mặt người',
    insight_ids: [],
  })).not.toThrow();
});
it('allows brief_draft to brief_ready', () => {
  expect(() => assertStageTransition('brief_draft', 'brief_ready')).not.toThrow();
});
it('allows brief_ready to scripting', () => {
  expect(() => assertStageTransition('brief_ready', 'scripting')).not.toThrow();
});
it('still blocks brief_draft to keyframing', () => {
  expect(() => assertStageTransition('brief_draft', 'keyframing')).toThrow(/stage_guard/);
});
```

Vitest:

```ts
expect(vdProjectBriefPath(7)).toBe('/api/v1/vd/projects/7/brief');
expect(vdProjectBriefReadyPath(7)).toBe('/api/v1/vd/projects/7/brief/ready');
expect(vdProjectBriefInsightsPath(7)).toBe('/api/v1/vd/projects/7/brief/insights');
```

- [ ] **Step 2:** Jest + Vitest FAIL (missing exports / old guard)

- [ ] **Step 3:** Implement service + HTTP + SC-03 + SC-02 links. Keep S1 `assertStageTransition('brief_draft','brief_draft')` green.

- [ ] **Step 4:** Jest brief + stage specs PASS. Vitest path helpers PASS.

- [ ] **Step 5:** `feat(vd): BR-01 brief eight groups and SC-03`

---

### Task 3: ITextGen director + SC-04 UI

**Files:**
- Create: `services/ptt-crm-api/src/video-sop/adapters/i-text-gen.ts`
- Create: `services/ptt-crm-api/src/video-sop/adapters/openai.text.ts`
- Test: `services/ptt-crm-api/src/video-sop/adapters/i-text-gen.spec.ts`
- Create: `services/ptt-crm-api/src/video-sop/script/vd-idea.repository.ts`
- Create: `services/ptt-crm-api/src/video-sop/script/vd-shot.repository.ts` (insert/list; feasibility wiring = Task 4 — this task may insert without FR assert)
- Create: `services/ptt-crm-api/src/video-sop/script/vd-script.service.ts`
- Test: `services/ptt-crm-api/src/video-sop/script/vd-script.service.spec.ts`
- Create: `services/ptt-crm-api/src/video-sop/script/vd-script.controller.ts`
- Modify: `vd-dispatcher.service.ts` — register `cine_director`
- Modify: `video-sop.module.ts`
- Create: `services/ops-web/src/app/crm/video/[id]/script/page.tsx`
- Modify: `video-sop-api.ts` + `.spec.ts`
- Guard: `StaffVdScriptEditGuard` — `crm_vd.script` edit **or** `crm_vd.project` edit **or** `crm_content` write

**Interfaces:**

```ts
export const STUB_IDEAS = [
  'Hook 3s + benefit + CTA',
  'Before/after + social proof',
  'Myth vs fact + offer',
] as const;

export interface ITextGen {
  readonly providerName: 'openai' | 'stub';
  complete(input: { system: string; user: string }): Promise<unknown>;
}
export function selectTextGen(env: { OPENAI_API_KEY: string }): ITextGen;
// nonempty key → openai; else stub that resolves
// { ideas: [{ summary: STUB_IDEAS[0] }, { summary: STUB_IDEAS[1] }, { summary: STUB_IDEAS[2] }] }

export type VdIdeaRow = {
  id: number; project_id: number; ordinal: number; summary: string; selected: boolean;
};
```

HTTP:
- `POST /api/v1/vd/projects/:id/ideas/generate` header `Idempotency-Key` required → job `q.text` `cine_director` → **201** `{ id, status: 'queued' }`. Missing header → `idempotency_key_required`. Stage must be `brief_ready` or `ideation` (nếu `brief_ready` thì transition → `ideation` trước enqueue). Else `stage_guard`.
- Handler: `selectTextGen(env).complete(...)`. Parse 3 summaries (stub shape or `ideas[].summary`). Replace `vd_ideas` for that project with ordinal 1..3, `selected=false`. Unknown parse → job `failed` `validation`.
- `GET /api/v1/vd/projects/:id/ideas` → `VdIdeaRow[]`
- `POST /api/v1/vd/projects/:id/ideas/:ideaId/select` → set that idea `selected=true`, others false
- `GET /api/v1/vd/projects/:id/scripts` → script rows `{ id, project_id, version, markdown }`
- `POST /api/v1/vd/projects/:id/scripts` `{ markdown }` — insert version = max+1. Stage `brief_ready` or `ideation` → `scripting`. From `scripting` stay. Else `stage_guard`.
- `GET /api/v1/vd/scripts/:id/shots` → shot rows
- `POST /api/v1/vd/scripts/:id/shots` — Task 3 may accept one shot and insert `status=draft` **without** feasibility (Task 4 adds assert). Body fields: `duration_ms`, `camera`, `action`, `aspect?`, `contains_human?`, `text_in_frame?`, `logo_in_ai_frame?`, `seed?`
- `GET /api/v1/vd/prompt-templates` → `{ code, kind, body }[]` (view guard)

SC-04 — 3 columns CSS `display:grid; grid-template-columns: 1fr 1fr 1fr; gap: 1rem` (stack on narrow via existing page styles if any; default 3 col):
- Title: `Script Studio (SC-04)`
- Banner **exact**: `S3 — 3 cột template · ý tưởng · shotlist.`
- Flag off → `Module tắt`
- Col 1 heading **exact**: `Template` — list `code | kind` from GET templates; empty `Chưa có template — seed DDL S3.`
- Col 2 heading **exact**: `Ý tưởng / Script`
  - Button **exact**: `Sinh 3 ý tưởng` (hidden without script/project edit / content write)
  - After 201: reload ideas (poll 1s × 8). Empty copy **exact**: `Chưa có ý tưởng — sinh 3 ý tưởng (S3).`
  - Each idea: `ordinal. summary` + button **exact**: `Chọn ý tưởng`
  - Selected idea: prefix `✓ `
  - Textarea markdown + button **exact**: `Lưu script`
- Col 3 heading **exact**: `Shotlist`
  - Fields to add one shot: `duration_ms`, `camera`, `action`, `aspect` (select `9:16`/`1:1`)
  - Button **exact**: `Thêm shot`
  - Table: `ordinal | duration_ms | camera | action | aspect | status | feasibility`
  - Feasibility cell may be `—` until Task 4
  - Empty: `Chưa có shot.`
- Auth/refresh same as SC-02. No fake CDN.

Path helpers:

```ts
expect(vdProjectIdeasPath(7)).toBe('/api/v1/vd/projects/7/ideas');
expect(vdProjectIdeasGeneratePath(7)).toBe('/api/v1/vd/projects/7/ideas/generate');
expect(vdProjectScriptsPath(7)).toBe('/api/v1/vd/projects/7/scripts');
expect(vdScriptShotsPath(3)).toBe('/api/v1/vd/scripts/3/shots');
expect(vdPromptTemplatesPath()).toBe('/api/v1/vd/prompt-templates');
```

- [ ] **Step 1: Tests**

```ts
it('selects stub when OPENAI_API_KEY empty', () => {
  expect(selectTextGen({ OPENAI_API_KEY: '' }).providerName).toBe('stub');
});
it('writes three stub ideas ordinal 1..3', async () => {
  const rows = await scriptService.materializeStubIdeas(1);
  expect(rows.map((r) => r.ordinal)).toEqual([1, 2, 3]);
  expect(rows.map((r) => r.summary)).toEqual([...STUB_IDEAS]);
});
```

- [ ] **Step 2:** FAIL

- [ ] **Step 3:** Adapters + dispatcher handler + HTTP + SC-04. Grep: no `social-ffmpeg` / `social-video.service` / `market-research/`.

- [ ] **Step 4:** Jest + Vitest PASS

- [ ] **Step 5:** `feat(vd): director ideas and SC-04 script studio`

---

### Task 4: Feasibility FR-R01…10 + shot POST assert

**Files:**
- Create: `services/ptt-crm-api/src/video-sop/rules/vd-feasibility.rules.ts`
- Test: `services/ptt-crm-api/src/video-sop/rules/vd-feasibility.rules.spec.ts`
- Modify: `vd-script.service.ts` — POST shots calls assert
- Test: add cases on `vd-script.service.spec.ts` for `feasibility_blocked`

**Interfaces:**

```ts
export type VdShotDraft = {
  duration_ms: number;
  text_in_frame: boolean;
  contains_human: boolean | unknown;
  aspect: string;
  camera: string;
  action: string;
  logo_in_ai_frame: boolean;
  seed: number | null;
  status: string;
};

export function evaluateFeasibility(
  project: { duration_sec: number; platform: string },
  shots: VdShotDraft[],
): { id: string; ok: boolean }[];

export function assertFeasibilityPass(
  project: { duration_sec: number; platform: string },
  shots: VdShotDraft[],
): void; // throws Error('feasibility_blocked') if any ok===false

export function assertShotFeasibility(shot: VdShotDraft): void;
// evaluate with dummy project { duration_sec: 30, platform: 'reels' } and [shot]
// throw feasibility_blocked if any of FR-R01,R02,R03,R04,R06,R07,R08 is ok===false
// ignore FR-R05,R09,R10 for single-shot
```

Fail table (parent khóa):

| ID | Fail khi |
|----|----------|
| FR-R01 | `shot.duration_ms > 15000` |
| FR-R02 | `shot.text_in_frame === true` |
| FR-R03 | `shot.contains_human` không phải boolean |
| FR-R04 | `shot.aspect` không thuộc `9:16` hoặc `1:1` |
| FR-R05 | số shot < 3 hoặc > 12 |
| FR-R06 | `camera` hoặc `action` không phải string độ dài ≥ 3 |
| FR-R07 | `shot.logo_in_ai_frame === true` |
| FR-R08 | status `keyframe_approved` mà `seed` null |
| FR-R09 | `project.duration_sec` ngoài 15–60 |
| FR-R10 | `platform` không thuộc `reels` / `shorts` / `feed_square` |

HTTP:
- POST one shot → `assertShotFeasibility` then insert `draft`. Fail → 400 `feasibility_blocked` (no row).
- POST `{ shots: VdShotDraft[] }` → `assertFeasibilityPass` using brief `duration_sec`/`platform` (default 30/`reels` if brief empty) then replace/insert all. Fail → 400, no write.
- GET shots may include `feasibility: { id, ok }[]` per shot (per-shot rules only) for SC-04 badge.

SC-04 feasibility column: `OK` if all per-shot rules ok; else first failing id e.g. `FR-R01`.

- [ ] **Step 1:** Ten `it('FR-R0x ...')` — write **all ten** in the spec file (do not write “tương tự”).

```ts
it('FR-R01 fails when duration_ms is 16000', () => {
  const rows = evaluateFeasibility({ duration_sec: 30, platform: 'reels' }, [{
    duration_ms: 16000, text_in_frame: false, contains_human: false, aspect: '9:16',
    camera: 'push in', action: 'walk', logo_in_ai_frame: false, seed: 1, status: 'draft',
  }]);
  expect(rows.find((r) => r.id === 'FR-R01')?.ok).toBe(false);
});
it('FR-R02 fails when text_in_frame is true', () => {
  const rows = evaluateFeasibility({ duration_sec: 30, platform: 'reels' }, [{
    duration_ms: 3000, text_in_frame: true, contains_human: false, aspect: '9:16',
    camera: 'push in', action: 'walk', logo_in_ai_frame: false, seed: 1, status: 'draft',
  }]);
  expect(rows.find((r) => r.id === 'FR-R02')?.ok).toBe(false);
});
it('FR-R03 fails when contains_human is not boolean', () => {
  const rows = evaluateFeasibility({ duration_sec: 30, platform: 'reels' }, [{
    duration_ms: 3000, text_in_frame: false, contains_human: 'yes', aspect: '9:16',
    camera: 'push in', action: 'walk', logo_in_ai_frame: false, seed: 1, status: 'draft',
  }]);
  expect(rows.find((r) => r.id === 'FR-R03')?.ok).toBe(false);
});
it('FR-R04 fails when aspect is 16:9', () => {
  const rows = evaluateFeasibility({ duration_sec: 30, platform: 'reels' }, [{
    duration_ms: 3000, text_in_frame: false, contains_human: false, aspect: '16:9',
    camera: 'push in', action: 'walk', logo_in_ai_frame: false, seed: 1, status: 'draft',
  }]);
  expect(rows.find((r) => r.id === 'FR-R04')?.ok).toBe(false);
});
it('FR-R05 fails when shot count is 1', () => {
  const rows = evaluateFeasibility({ duration_sec: 30, platform: 'reels' }, [{
    duration_ms: 3000, text_in_frame: false, contains_human: false, aspect: '9:16',
    camera: 'push in', action: 'walk', logo_in_ai_frame: false, seed: 1, status: 'draft',
  }]);
  expect(rows.find((r) => r.id === 'FR-R05')?.ok).toBe(false);
});
it('FR-R06 fails when camera is empty', () => {
  const rows = evaluateFeasibility({ duration_sec: 30, platform: 'reels' }, [{
    duration_ms: 3000, text_in_frame: false, contains_human: false, aspect: '9:16',
    camera: '', action: 'walk', logo_in_ai_frame: false, seed: 1, status: 'draft',
  }]);
  expect(rows.find((r) => r.id === 'FR-R06')?.ok).toBe(false);
});
it('FR-R07 fails when logo_in_ai_frame is true', () => {
  const rows = evaluateFeasibility({ duration_sec: 30, platform: 'reels' }, [{
    duration_ms: 3000, text_in_frame: false, contains_human: false, aspect: '9:16',
    camera: 'push in', action: 'walk', logo_in_ai_frame: true, seed: 1, status: 'draft',
  }]);
  expect(rows.find((r) => r.id === 'FR-R07')?.ok).toBe(false);
});
it('FR-R08 fails when keyframe_approved and seed null', () => {
  const rows = evaluateFeasibility({ duration_sec: 30, platform: 'reels' }, [{
    duration_ms: 3000, text_in_frame: false, contains_human: false, aspect: '9:16',
    camera: 'push in', action: 'walk', logo_in_ai_frame: false, seed: null, status: 'keyframe_approved',
  }]);
  expect(rows.find((r) => r.id === 'FR-R08')?.ok).toBe(false);
});
it('FR-R09 fails when duration_sec is 10', () => {
  const rows = evaluateFeasibility({ duration_sec: 10, platform: 'reels' }, [{
    duration_ms: 3000, text_in_frame: false, contains_human: false, aspect: '9:16',
    camera: 'push in', action: 'walk', logo_in_ai_frame: false, seed: 1, status: 'draft',
  }]);
  expect(rows.find((r) => r.id === 'FR-R09')?.ok).toBe(false);
});
it('FR-R10 fails when platform is tiktok', () => {
  const rows = evaluateFeasibility({ duration_sec: 30, platform: 'tiktok' }, [{
    duration_ms: 3000, text_in_frame: false, contains_human: false, aspect: '9:16',
    camera: 'push in', action: 'walk', logo_in_ai_frame: false, seed: 1, status: 'draft',
  }]);
  expect(rows.find((r) => r.id === 'FR-R10')?.ok).toBe(false);
});
```

- [ ] **Step 2:** `./node_modules/.bin/jest src/video-sop/rules/vd-feasibility.rules.spec.ts --no-coverage` — FAIL

- [ ] **Step 3:** Ten pure functions in one file + wire shot POST.

- [ ] **Step 4:** 10 tests PASS + script service blocked-shot test PASS

- [ ] **Step 5:** `feat(vd): feasibility FR-R01 to FR-R10`

---

### Task 5: Smoke S3 + user guide

**Files:**
- Create: `scripts/smoke_video_sop_s3.sh`
- Create: `docs/huong-dan-su-dung/19-video-sop.md`
- Modify: `docs/huong-dan-su-dung/README.md` row 19 → link `19-video-sop.md`

**Interfaces:** Auth như `scripts/smoke_video_sop_s2.sh`. `LIFECYCLE_ID` default 3. Reuse existing project if list nonempty (daily cap).

Steps:
1. Flag cinematic off → `SKIP cinematic off` exit 0
2. Reuse or create S1 project
3. `PUT /brief` missing `objective` (or short) then `POST /brief/ready` → 400 `brief_incomplete`
4. `PUT /brief` eight groups complete (`insight_ids: []`) then `POST /brief/ready` → 200 and `stage=brief_ready`
5. `POST /projects/:id/scripts` `{ markdown: "S3 smoke" }` from `brief_ready` → 200/201 and stage `scripting` (ideas generate optional for smoke).
6. `POST /scripts/:id/shots` with `duration_ms=20000`, `camera=push in`, `action=walk`, `aspect=9:16` → 400 `feasibility_blocked`
7. POST valid shot `duration_ms=3000` `camera=push in` `action=walk` `aspect=9:16` → 201/200 `status=draft`
8. Echo `OK Video SOP S3 — project #$ID`
9. Do not dump OpenAI/provider bodies. Do not print DATABASE_URL.

Guide `19-video-sop.md` (Vietnamese, short):
- Hub `/crm/video` · Overview `/crm/video/[id]`
- SC-03: 8 nhóm, Lưu brief, Đánh dấu brief sẵn sàng, insight được trống
- SC-04: Sinh 3 ý tưởng, Chọn ý tưởng, Lưu script, Thêm shot, badge FR-R0x
- Phân biệt Video tuần (Media AI) vs Video chiến dịch (hub này)
- Flag + daily cap
- S3 chưa có Gate 1

- [ ] **Step 1:** Script `set -euo pipefail`

- [ ] **Step 2:** Flag 0 → SKIP. Flag 1 → OK or documented 400s.

- [ ] **Step 3:** No production API besides brief/script/shots/ideas (guide only).

- [ ] **Step 4:** `bash scripts/smoke_video_sop_s3.sh` exit 0 when cinematic off (SKIP). If local API down, script may fail — report, do not fake OK.

- [ ] **Step 5:** `test(vd): S3 smoke brief and feasibility`

---

## Spec coverage (self-review)

| Spec | Task |
|------|------|
| BR-01 8 nhóm + insight_ids[] | 2 |
| SC-03 `/crm/video/[id]/brief` | 2 |
| SC-04 3 cột template \| LLM \| shotlist | 3 |
| ITextGen stub/OpenAI, no image | 3 |
| FR-R01…10 | 4 |
| M6 insight picker (optional []) | 2 |
| Stage S3 only, no Gate | 2, 3, 5 |
| AC-R1 no `social_*` | 3, 5 |
| Guide 19 | 5 |
| Bible/Gate/Kling/SC-05+ | **out** |

## Placeholder scan

Không TBD. `brief_ready→scripting` khóa ở Global Constraints + Task 2/3 (smoke Task 5 dùng lại).

## Type consistency

- Brief keys: `objective` `audience` `offer` `duration_sec` `platform` `tone` `constraints` `insight_ids`
- Platforms: `reels` `shorts` `feed_square`
- Job: `cine_director` / `q.text`
- Errors: `brief_incomplete` `feasibility_blocked` `stage_guard` `idempotency_key_required` `cmkt_cinematic_disabled` `vd_tables_missing`
- Stub ideas: three exact strings above
- UI strings copied verbatim in Tasks 2–3
