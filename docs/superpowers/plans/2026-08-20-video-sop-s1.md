# Video SOP Module 7 — Sprint 1 (Cinematic A) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mở **Video chiến dịch (SOP)** ở mức S1: bảng `vd_*` tối thiểu, Nest `VideoSopModule`, hub SC-01/02, picker Content OS tạo `vd_projects` + khóa studio `cinematic` — **không** Leonardo/Kling, **không** Gate 2–4, **không** import `video-social`.

**Architecture:** Sibling Nest module `src/video-sop/` (ADR-R1). API `/api/v1/vd`. Content item chỉ giữ `media_json.video_studio=cinematic` + `vd_project_id`. Stage machine bắt đầu `brief_draft`. Social FFmpeg giữ nguyên.

**Tech Stack:** NestJS `ptt-crm-api`, Next.js `ops-web`, PostgreSQL `vd_*`, Jest, RBAC `crm_vd.*`.

**Spec:** [`2026-08-20-video-sop-module-7-design.md`](../specs/2026-08-20-video-sop-module-7-design.md) §15 S1 · [`2026-08-20-cmkt-video-dual-studio-design.md`](../specs/2026-08-20-cmkt-video-dual-studio-design.md) Cinematic A

**Plan cả module S1–S10:** [`2026-08-20-video-sop-module-7.md`](./2026-08-20-video-sop-module-7.md) — file này = TDD chi tiết Tasks 1–9.

## Global Constraints

- **Không** import `content-marketing/video-social/**` từ `video-sop/**` (EC-DUAL-06, AC-R1).
- **Không** tạo job `social_*` hay `cine_*` trong S1. Không gọi Leonardo/Kling/Runway/Topaz.
- **Flag:** `PTT_CMKT_VIDEO_CINEMATIC=1` (API) + `NEXT_PUBLIC_CMKT_VIDEO_CINEMATIC=1` (ops-web). Flag tắt → picker SOP disabled, `/crm/video` 404/ẩn.
- **Quota S1:** `PTT_CMKT_VIDEO_CINEMATIC_DAILY_CAP` default `1` — đếm `vd_projects` created hôm nay theo `lifecycle_id`, không đếm social jobs.
- **API prefix:** `/api/v1/vd` — không nhét vào `/content-marketing/jobs`.
- **Caps:** `crm_vd.project` view/edit/create; S1 chưa seed `crm_vd.gate1` approve (S5).
- **Stage S1:** chỉ `brief_draft`. Mọi chuyển stage khác → 400 `stage_guard`.
- **Lock studio:** item đã `video_studio=social` + job → `studio_locked`. Cinematic lock **không** gọi `SocialVideoService.lockStudio`.
- **Commit:** chỉ khi user yêu cầu. TDD: test fail trước, rồi code tối thiểu.

---

## File map

```
docs/specs/postgresql-ddl-vd-sop-s1.sql
scripts/apply_pg_ddl_vd_sop_s1.sh
scripts/smoke_video_sop_s1.sh

services/ptt-crm-api/src/video-sop/
  video-sop.module.ts
  video-sop.types.ts
  video-sop-flags.ts
  video-sop-flags.spec.ts
  rules/vd-stage.guard.ts
  rules/vd-stage.guard.spec.ts
  project/vd-project.repository.ts
  project/vd-project.service.ts
  project/vd-project.service.spec.ts
  project/vd-project.controller.ts
  guards/staff-vd-project.guard.ts
  audit/vd-audit.service.ts

Modify:
  services/ptt-crm-api/src/config/app-config.service.ts
  services/ptt-crm-api/src/app.module.ts
  services/ptt-crm-api/src/staff-permissions/rbac-admin-catalog.json
  services/ptt-crm-api/src/content-marketing/content-marketing.types.ts   # vd_project_id
  services/ptt-crm-api/src/content-marketing/video-social/social-video.service.ts
    # lockStudio giữ chỉ social — không thêm cinematic ở đây

services/ops-web/src/
  lib/video-sop-api.ts
  lib/video-sop-api.spec.ts
  app/crm/video/page.tsx                         # SC-01
  app/crm/video/[id]/page.tsx                    # SC-02
  components/content-os/ContentOsVideoStudioPicker.tsx
  components/OpsNav.tsx
  components/layout/nav-icons.tsx
```

**Ngoài S1 (plan sau):** SC-03…16, Gate 1–4, shots, bible, Leonardo, Kling, cost, portal.

---

### Task 1: Flags + daily cap

**Files:**
- Modify: `services/ptt-crm-api/src/config/app-config.service.ts` (cạnh `contentMarketingVideoSocialEnabled`)
- Create: `services/ptt-crm-api/src/video-sop/video-sop-flags.ts`
- Test: `services/ptt-crm-api/src/video-sop/video-sop-flags.spec.ts`

**Interfaces:**
- Consumes: `process.env.PTT_CMKT_VIDEO_CINEMATIC`, `PTT_CMKT_VIDEO_CINEMATIC_DAILY_CAP`
- Produces:
  - `AppConfigService.contentMarketingVideoCinematicEnabled: boolean` — default **false**
  - `AppConfigService.contentMarketingVideoCinematicDailyCap: number` — default `1`, floor ≥1
  - `assertCinematicEnabled(config): void` ném `{ error: 'cmkt_cinematic_disabled' }`

- [ ] **Step 1: Test fail**

```ts
import { assertCinematicEnabled } from './video-sop-flags';

describe('video-sop-flags', () => {
  it('throws cmkt_cinematic_disabled when flag off', () => {
    expect(() =>
      assertCinematicEnabled({ contentMarketingVideoCinematicEnabled: false }),
    ).toThrow(/cmkt_cinematic_disabled/);
  });

  it('passes when flag on', () => {
    expect(() =>
      assertCinematicEnabled({ contentMarketingVideoCinematicEnabled: true }),
    ).not.toThrow();
  });
});
```

- [ ] **Step 2:** `cd services/ptt-crm-api && ./node_modules/.bin/jest src/video-sop/video-sop-flags.spec.ts --no-coverage`  
  Expected: FAIL module not found

- [ ] **Step 3: Implement**

```ts
export function assertCinematicEnabled(config: { contentMarketingVideoCinematicEnabled: boolean }): void {
  if (!config.contentMarketingVideoCinematicEnabled) {
    throw new Error('cmkt_cinematic_disabled');
  }
}
```

Trong `AppConfigService` constructor (sau social cap):

```ts
this.contentMarketingVideoCinematicEnabled = ['1', 'true', 'yes', 'on'].includes(
  (process.env.PTT_CMKT_VIDEO_CINEMATIC ?? '0').trim().toLowerCase(),
);
const cineCapRaw = Number(process.env.PTT_CMKT_VIDEO_CINEMATIC_DAILY_CAP ?? 1);
this.contentMarketingVideoCinematicDailyCap =
  Number.isFinite(cineCapRaw) && cineCapRaw > 0 ? Math.floor(cineCapRaw) : 1;
```

Khai báo `readonly` hai field cạnh social cap.

- [ ] **Step 4:** Jest PASS

- [ ] **Step 5:** Commit chỉ khi user yêu cầu — `feat(vd): cinematic flag and daily cap`

---

### Task 2: DDL S1

**Files:**
- Create: `docs/specs/postgresql-ddl-vd-sop-s1.sql`
- Create: `scripts/apply_pg_ddl_vd_sop_s1.sh` (copy pattern `scripts/apply_pg_ddl_cmkt_video_social_v1.sh`)

**Interfaces:**
- Produces tables: `vd_projects`, `vd_briefs`, `vd_scripts`, `vd_audit_logs`
- **Không** tạo `vd_shots`, `vd_jobs`, `vd_gates` đầy đủ — Gate 1 = S5. S1 chỉ cần project + brief stub + audit.

- [ ] **Step 1:** SQL (áp nguyên):

```sql
CREATE TABLE IF NOT EXISTS vd_projects (
  id              bigserial PRIMARY KEY,
  lifecycle_id    bigint NOT NULL,
  client_id       text,
  cmkt_item_id    bigint,
  title           text NOT NULL,
  stage           text NOT NULL DEFAULT 'brief_draft'
                    CHECK (stage IN (
                      'brief_draft','brief_ready','ideation','scripting','shotlist_ready',
                      'keyframing','animating','post_production','delivered','archived'
                    )),
  status          text NOT NULL DEFAULT 'active'
                    CHECK (status IN ('active','on_hold','cancelled')),
  created_by      text NOT NULL,
  created_at      timestamptz NOT NULL DEFAULT now(),
  updated_at      timestamptz NOT NULL DEFAULT now(),
  deleted_at      timestamptz
);
CREATE UNIQUE INDEX IF NOT EXISTS vd_projects_cmkt_item_uidx
  ON vd_projects (cmkt_item_id) WHERE cmkt_item_id IS NOT NULL AND deleted_at IS NULL;
CREATE INDEX IF NOT EXISTS vd_projects_lifecycle_idx ON vd_projects (lifecycle_id) WHERE deleted_at IS NULL;

CREATE TABLE IF NOT EXISTS vd_briefs (
  id              bigserial PRIMARY KEY,
  project_id      bigint NOT NULL REFERENCES vd_projects(id),
  body_json       jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at      timestamptz NOT NULL DEFAULT now(),
  updated_at      timestamptz NOT NULL DEFAULT now()
);
CREATE UNIQUE INDEX IF NOT EXISTS vd_briefs_project_uidx ON vd_briefs (project_id);

CREATE TABLE IF NOT EXISTS vd_scripts (
  id              bigserial PRIMARY KEY,
  project_id      bigint NOT NULL REFERENCES vd_projects(id),
  version         int NOT NULL DEFAULT 1,
  markdown        text NOT NULL DEFAULT '',
  created_at      timestamptz NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS vd_audit_logs (
  id              bigserial PRIMARY KEY,
  project_id      bigint NOT NULL REFERENCES vd_projects(id),
  actor_email     text NOT NULL,
  action          text NOT NULL,
  payload_json    jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at      timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX IF NOT EXISTS vd_audit_project_idx ON vd_audit_logs (project_id, created_at DESC);
```

- [ ] **Step 2:** Apply script đọc `DATABASE_URL` từ `.env`, `psql -v ON_ERROR_STOP=1 -f docs/specs/postgresql-ddl-vd-sop-s1.sql`

- [ ] **Step 3:** Chạy apply trên local/dev. Expected: exit 0, `\dt vd_*` hiện 4 bảng

- [ ] **Step 4:** Không test Jest — DDL verify bằng `psql`

- [ ] **Step 5:** Commit khi user yêu cầu — `feat(vd): S1 DDL projects briefs scripts audit`

---

### Task 3: Stage guard

**Files:**
- Create: `services/ptt-crm-api/src/video-sop/rules/vd-stage.guard.ts`
- Test: `services/ptt-crm-api/src/video-sop/rules/vd-stage.guard.spec.ts`

**Interfaces:**
- Produces: `assertStageTransition(from: VdProjectStage, to: VdProjectStage): void`
- S1 allowed: **không** transition nào (chỉ tạo ở `brief_draft`). Mọi `from !== to` → `stage_guard`.
- Export union `VdProjectStage` khớp CHECK SQL.

- [ ] **Step 1: Test**

```ts
import { assertStageTransition } from './vd-stage.guard';

describe('vd-stage.guard', () => {
  it('allows staying on brief_draft', () => {
    expect(() => assertStageTransition('brief_draft', 'brief_draft')).not.toThrow();
  });

  it('blocks S1 jump to keyframing', () => {
    expect(() => assertStageTransition('brief_draft', 'keyframing')).toThrow(/stage_guard/);
  });
});
```

- [ ] **Step 2:** Jest FAIL module not found

- [ ] **Step 3:**

```ts
export type VdProjectStage =
  | 'brief_draft' | 'brief_ready' | 'ideation' | 'scripting' | 'shotlist_ready'
  | 'keyframing' | 'animating' | 'post_production' | 'delivered' | 'archived';

export function assertStageTransition(from: VdProjectStage, to: VdProjectStage): void {
  if (from === to) return;
  throw new Error('stage_guard');
}
```

- [ ] **Step 4:** Jest PASS

- [ ] **Step 5:** `feat(vd): S1 stage guard blocks all advances`

---

### Task 4: Repository + create project + cap

**Files:**
- Create: `services/ptt-crm-api/src/video-sop/video-sop.types.ts`
- Create: `services/ptt-crm-api/src/video-sop/project/vd-project.repository.ts`
- Create: `services/ptt-crm-api/src/video-sop/project/vd-project.service.ts`
- Test: `services/ptt-crm-api/src/video-sop/project/vd-project.service.spec.ts`

**Interfaces:**
- Consumes: `AppConfigService`, `VdProjectRepository`, `assertCinematicEnabled`, `assertStageTransition`
- Produces:
  - `VdProjectRow { id, lifecycle_id, client_id, cmkt_item_id, title, stage, status, created_by, created_at, updated_at }`
  - `createFromContentItem({ lifecycleId, itemId, title, scriptMarkdown, email, clientId }): Promise<VdProjectRow>`
  - `listByLifecycle(lifecycleId): Promise<VdProjectRow[]>`
  - `getById(id): Promise<VdProjectRow>`
  - Daily cap: `countCreatedToday(lifecycleId) >= cinematicDailyCap` → ném `video_cinematic_daily_cap`
  - Unique `cmkt_item_id`: đã có project → trả row hiện có (idempotent), không tạo thứ hai

`createFromContentItem` steps:
1. `assertCinematicEnabled`
2. count today vs cap
3. insert project `stage=brief_draft`
4. insert empty `vd_briefs.body_json={}`
5. insert `vd_scripts` version 1 với `markdown=scriptMarkdown`
6. insert `vd_audit_logs` action `project.created`

- [ ] **Step 1: Test** (mock repo)

```ts
it('rejects when cinematic flag off', async () => {
  const svc = makeSvc({ enabled: false, cap: 1, today: 0 });
  await expect(
    svc.createFromContentItem({
      lifecycleId: 3, itemId: 12, title: 'Chiến dịch', scriptMarkdown: 'Hook', email: 'a@b.c',
    }),
  ).rejects.toThrow(/cmkt_cinematic_disabled/);
});

it('rejects when daily cap reached', async () => {
  const svc = makeSvc({ enabled: true, cap: 1, today: 1 });
  await expect(
    svc.createFromContentItem({
      lifecycleId: 3, itemId: 12, title: 'Chiến dịch', scriptMarkdown: 'Hook', email: 'a@b.c',
    }),
  ).rejects.toThrow(/video_cinematic_daily_cap/);
});

it('returns existing project for same cmkt_item_id', async () => {
  const existing = { id: 7, cmkt_item_id: 12, stage: 'brief_draft' };
  const svc = makeSvc({ enabled: true, cap: 1, today: 0, existing });
  const row = await svc.createFromContentItem({
    lifecycleId: 3, itemId: 12, title: 'Chiến dịch', scriptMarkdown: 'Hook', email: 'a@b.c',
  });
  expect(row.id).toBe(7);
  expect(svc.repo.insertProject).not.toHaveBeenCalled();
});
```

- [ ] **Step 2:** Jest FAIL

- [ ] **Step 3:** Implement service + in-memory repo cho test; PG repo `INSERT … RETURNING` khi `ensurePgReady`

- [ ] **Step 4:** Jest PASS

- [ ] **Step 5:** `feat(vd): create project from content item with daily cap`

---

### Task 5: HTTP `/api/v1/vd` + lock cinematic trên item

**Files:**
- Create: `services/ptt-crm-api/src/video-sop/project/vd-project.controller.ts`
- Create: `services/ptt-crm-api/src/video-sop/guards/staff-vd-project.guard.ts`
- Create: `services/ptt-crm-api/src/video-sop/video-sop.module.ts`
- Modify: `services/ptt-crm-api/src/app.module.ts` — `imports: [VideoSopModule]`
- Modify: `services/ptt-crm-api/src/content-marketing/content-marketing.types.ts` — `vd_project_id?: number` trên `CmktMediaJson`
- Modify: Content OS patch item helper (dùng `ContentMarketingRepository.patchItem`) — **inject repo**, không import `SocialVideoService`

**Interfaces:**
- `POST /api/v1/vd/projects` body `{ lifecycle_id, cmkt_item_id, title? }`
  - Guard: staff + `crm_vd.project` create **hoặc** `crm_content` write (fallback S1 nếu cap chưa seed)
  - Load content item; `assertStudioWritable(item.media_json, 'cinematic')` từ `video-social/social-studio.util` **được** import (kernel-level lock helper — không import composer/jobs)
  - Tạo project; `patchItem` `{ media_json: { …lockVideoStudio(media,'cinematic'), vd_project_id: row.id } }`
  - 201 + `VdProjectRow`
- `GET /api/v1/vd/projects?lifecycle_id=` → list
- `GET /api/v1/vd/projects/:id` → row
- Social `lockStudio` **giữ** `if (studio !== 'social') throw studio_mismatch` — cinematic không đi route đó

- [ ] **Step 1: Controller spec** (supertest hoặc service-level): cinematic create sets `video_studio=cinematic` và `vd_project_id`; social item already locked → 400 `studio_locked`

- [ ] **Step 2:** FAIL

- [ ] **Step 3:** Implement controller + module. Guard đọc `StaffAuthService.hasCap(me.caps, 'crm_vd.project', 'create')` OR `crm_content` `write`.

```ts
@Controller('api/v1/vd/projects')
export class VdProjectController {
  @Post()
  @HttpCode(201)
  create(@Body() body: Record<string, unknown>, @Req() req: Request) { /* … */ }

  @Get()
  list(@Query('lifecycle_id', ParseIntPipe) lifecycleId: number) { /* … */ }

  @Get(':id')
  get(@Param('id', ParseIntPipe) id: number) { /* … */ }
}
```

- [ ] **Step 4:** Jest PASS. Grep: `video-sop` không import `social-ffmpeg` / `social-video.service`

- [ ] **Step 5:** `feat(vd): POST /api/v1/vd/projects locks cinematic studio`

---

### Task 6: Caps catalog

**Files:**
- Modify: `services/ptt-crm-api/src/staff-permissions/rbac-admin-catalog.json`

**Interfaces:**
- Section `crm_vd.project` actions `view`, `edit`, `create`
- Group: `CRM — Triển khai dịch vụ`, page `/crm/video`

- [ ] **Step 1:** Thêm vào `sections` map:

```json
"crm_vd.project": ["view", "edit", "create"]
```

Và object:

```json
{
  "id": "crm_vd.project",
  "label": "Video SOP — Project",
  "group": "CRM — Triển khai dịch vụ",
  "page": "/crm/video",
  "description": "Hub /crm/video: list + tạo project chiến dịch (Module 7 S1)."
}
```

Thêm `"crm_vd.project"` vào mảng id cuối catalog.

- [ ] **Step 2:** Nếu catalog có schema test, chạy test đó. Không thì diff thủ công JSON valid.

- [ ] **Step 3:** Admin gán cap cho role Creative Lead / AM trên staging sau deploy (runbook trong smoke).

- [ ] **Step 4:** JSON parse OK

- [ ] **Step 5:** `feat(vd): RBAC crm_vd.project`

---

### Task 7: ops-web hub SC-01 / SC-02

**Files:**
- Create: `services/ops-web/src/lib/video-sop-api.ts`
- Test: `services/ops-web/src/lib/video-sop-api.spec.ts` (vitest — theo `content-os-api.spec.ts`)
- Create: `services/ops-web/src/app/crm/video/page.tsx`
- Create: `services/ops-web/src/app/crm/video/[id]/page.tsx`

**Interfaces:**
- `VIDEO_SOP_API.createProject(token, { lifecycle_id, cmkt_item_id, title })` → `POST /api/v1/vd/projects`
- `listProjects(token, lifecycleId)` → `GET /api/v1/vd/projects?lifecycle_id=`
- `getProject(token, id)` → `GET /api/v1/vd/projects/:id`

SC-01: bảng `title | stage | status | item | updated`. Empty state: “Chọn Video chiến dịch từ Content Board”. Không form beat.

SC-02: hiện `id`, `stage=brief_draft`, `cmkt_item_id`, link lifecycle Content OS. Banner: “S1 — Brief/Gate 1 chưa mở (S3/S5)”.

Ẩn cả hai page khi `NEXT_PUBLIC_CMKT_VIDEO_CINEMATIC !== '1'` (render “Module tắt”).

- [ ] **Step 1:** Vitest path helpers

```ts
expect(vdProjectCreatePath()).toBe('/api/v1/vd/projects');
expect(vdProjectGetPath(7)).toBe('/api/v1/vd/projects/7');
```

- [ ] **Step 2:** FAIL

- [ ] **Step 3:** Implement API client + 2 pages (server/client theo pattern `/crm/sop`)

- [ ] **Step 4:** Vitest PASS

- [ ] **Step 5:** `feat(vd): SC-01 SC-02 hub /crm/video`

---

### Task 8: Picker + sidebar

**Files:**
- Modify: `services/ops-web/src/components/content-os/ContentOsVideoStudioPicker.tsx`
- Modify: `services/ops-web/src/components/content-os/ContentOsVideoStudioPicker.spec.ts`
- Modify: `services/ops-web/src/components/OpsNav.tsx`
- Modify: `services/ops-web/src/components/layout/nav-icons.tsx`

**Interfaces:**
- `pickCinematic`: gọi `createVdProject` (không `lockVideoStudio(..., 'cinematic')` qua social route)
- Success: `onSelect('cinematic')` + `router.push(/crm/video/${id})`
- Sidebar: nếu `hasCap(user, 'crm_vd.project', 'view')` **hoặc** (`NEXT_PUBLIC_CMKT_VIDEO_CINEMATIC===1` && `hasCap(user, 'crm_content', 'view')`) → link `{ href: '/crm/video', label: 'Video SOP' }` trong section `CRM · Triển khai dịch vụ`
- Icon map `'/crm/video': 'play'` (hoặc icon có sẵn)

- [ ] **Step 1:** Update picker spec: khi flag `1`, click cinematic gọi create API mock — không gọi lock-studio social

- [ ] **Step 2:** FAIL (hiện `pickCinematic` chỉ `onSelect`)

- [ ] **Step 3:** Wire API + nav

- [ ] **Step 4:** Spec PASS. `OpsNav` có link khi cap đúng

- [ ] **Step 5:** `feat(vd): picker creates vd_project and nav Video SOP`

---

### Task 9: Smoke S1

**Files:**
- Create: `scripts/smoke_video_sop_s1.sh`

**Interfaces:** Auth giống `scripts/smoke_content_marketing_video_social_v1.sh` (internal key). `LIFECYCLE_ID` default 3.

Steps:
1. Skip nếu API flag cinematic off — echo `SKIP cinematic off`
2. Tạo content item video_script + approve
3. `POST /api/v1/vd/projects` `{ lifecycle_id, cmkt_item_id }`
4. Assert JSON `stage=brief_draft`, `id` số
5. GET item → `media_json.video_studio=cinematic`, `vd_project_id` khớp
6. `POST .../lock-studio { studio: social }` → 400 `studio_locked` hoặc `studio_mismatch`
7. `POST .../jobs/video-storyboard` → 400 `studio_mismatch`
8. Echo `OK Video SOP S1 — project #$ID`

- [ ] **Step 1:** Viết script `set -euo pipefail`

- [ ] **Step 2:** Chạy local với flag 0 → SKIP. Flag 1 + DDL → OK

- [ ] **Step 3:** Không code production thêm

- [ ] **Step 4:** Smoke exit 0

- [ ] **Step 5:** `test(vd): S1 smoke create project and block social jobs`

---

## Spec coverage (self-review)

| Spec | Task |
|------|------|
| ADR-R1/R2/R3 module + `/api/v1/vd` + `/crm/video` | 5, 7 |
| S1 DDL + Nest + SC-01/02 + audit | 2, 4, 5, 7 |
| AC-R2 ẩn hub thiếu cap/flag | 7, 8 |
| AC-R4 picker tạo `vd_projects` | 5, 8 |
| AC-R1 không `social_*` trong `vd_jobs` | S1 không có `vd_jobs` |
| EC-DUAL-03 cinematic không tạo `social_*` | 9 |
| EC-DUAL-04 lock sau chọn | 5, 9 |
| EC-DUAL-06 import graph | 5 step 4 grep |
| BR-01 brief 8 nhóm | **S3** — ngoài plan |
| Gate 1–4, Leonardo, Kling | **S5/S2/S6** — ngoài plan |
| Social V1 hotfix TTS/drawtext | **không** trong plan này |

## Placeholder scan

Không TBD. S1 không làm SC-03+, Gate, provider.

## Type consistency

- `VdProjectStage` = SQL CHECK
- Error strings: `cmkt_cinematic_disabled`, `video_cinematic_daily_cap`, `stage_guard`, `studio_locked`
- Body field `cmkt_item_id` (không `item_id` trên `/api/v1/vd`)
- Media: `video_studio: 'cinematic'`, `vd_project_id: number`
