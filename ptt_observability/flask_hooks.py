"""Flask observability hooks stub — Flask HTTP retired."""
from __future__ import annotations

from typing import Any


def register_flask_observability(app: Any) -> None:
    """No-op — Flask monolith removed."""
