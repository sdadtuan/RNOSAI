# Meta Enterprise B8.1 — Breakdown insights + granular RBAC
# Requires B8 DDL v4 + daily_performance; apply DDL v8 for breakdown table.

# Graph breakdown sync + API
PTT_META_INSIGHTS_BREAKDOWN=0
NEXT_PUBLIC_PTT_META_INSIGHTS_BREAKDOWN=0

# Phase 1 breakdown dimension (publisher_platform required)
PTT_META_BREAKDOWN_TYPES=publisher_platform

# Seed granular caps (Buyer vs Tracking vs AM approve):
#   python3 scripts/seed_staff_meta_rbac_b81.py

# Apply before enabling breakdown:
# ./scripts/apply_pg_ddl_v8_meta_insights_breakdown.sh

# RBAC matrix (enforced in guards):
# - Media Buyer (MKT-02): hub view, write submit — NO approve, NO tracking configure
# - Tracking (TECH-01): crm_agency.configure — conversion rules + pixel
# - AM/Lead (MKT-01): meta_campaign_write.approve
