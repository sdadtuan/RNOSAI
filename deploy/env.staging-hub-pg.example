# Staging Hub PG read cutover (Phase 3 Track D)
# Apply after ./scripts/migrate_sqlite_hub_sop_to_pg.py + 7d dual-read soak

PTT_HUB_READ_SOURCE=1
PTT_SOP_READ_SOURCE=1
# Dual-write SQLite → PG during soak; enable after stable:
# PTT_HUB_PG_PRIMARY=1

# Lead shadow — keep ON until Phase 2 write soak ≥30d:
# PTT_LEAD_SHADOW_SYNC=1
