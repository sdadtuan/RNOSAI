# Temporal local dev — worker + Nest signal bridge
PTT_TEMPORAL_ADDRESS=127.0.0.1:7233
PTT_TEMPORAL_NAMESPACE=default
PTT_TEMPORAL_TASK_QUEUE=ptt-agency

# Nest CRM API (set when temporal stack is up)
# export PTT_TEMPORAL_ADDRESS=127.0.0.1:7233
