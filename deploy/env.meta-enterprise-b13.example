# Meta Enterprise B13 — Ops webhooks (account disabled + ad disapproved alerts)
# Requires meta_alerts table (DDL v4) before enabling in production.

PTT_META_OPS_WEBHOOKS=0

# Hub alert table + badge also require:
PTT_META_ALERTS_ENABLED=1

# Existing webhook route (Nest):
# POST /api/v1/webhooks/meta
# PTT_WEBHOOKS_NEST_META=1
