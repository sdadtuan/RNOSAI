# Meta / Facebook Ads — Flask admin retired → ops-web (Horizon 1 B3.3)
# Include inside rs.pttads.vn server { } block (before Flask catch-all).
location ^~ /crm/facebook-ads {
    return 302 https://ops.pttads.vn/meta/facebook-ads;
}
